---
phase: 01-auth-database-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - .gitignore
  - .env.example
  - drizzle.config.ts
  - vitest.config.ts
  - src/db/schema.ts
  - src/db/migrations/
  - src/lib/db.ts
  - src/tests/setup.ts
  - src/tests/db.test.ts
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04]
user_setup:
  - service: neon
    why: "Postgres database — schema must be applied to a live Neon project before migration task can run"
    env_vars:
      - name: DATABASE_URL
        source: "Neon Console -> Project -> Connection Details -> connection string (with ?sslmode=require)"
    dashboard_config:
      - task: "Create a free Neon project (no credit card)"
        location: "https://neon.tech -> New Project"
  - service: github
    why: "GitHub OAuth App credentials needed by Plan 03 (collect now to unblock later wave)"
    env_vars:
      - name: GITHUB_CLIENT_ID
        source: "github.com/settings/developers -> OAuth Apps -> New OAuth App -> Client ID"
      - name: GITHUB_CLIENT_SECRET
        source: "github.com/settings/developers -> OAuth Apps -> Generate a new client secret"
    dashboard_config:
      - task: "Create OAuth App with callback URL http://localhost:3000/api/auth/callback/github and homepage http://localhost:3000"
        location: "github.com/settings/developers"

must_haves:
  truths:
    - "Project builds and lints cleanly (npm run build, npm run lint)"
    - "Two Drizzle instances exist: db (neon-http) for app queries, authDb (neon-serverless) for Better Auth"
    - "All 7 tables exist in Neon via an applied Drizzle migration"
    - "vitest runs and a DB connectivity test performs a real write + read against Neon"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "All 7 Drizzle tables (user, session, account, verification, project, project_member, invitation, ticket) + ticket_status enum + unique(project_id, ticket_number)"
      contains: "pgEnum('ticket_status'"
    - path: "src/lib/db.ts"
      provides: "Dual Neon driver instances"
      contains: "neon-serverless"
    - path: "drizzle.config.ts"
      provides: "drizzle-kit migration config pointing at src/db/schema.ts"
      contains: "./src/db/schema.ts"
    - path: "src/db/migrations"
      provides: "Generated SQL migration files"
    - path: "vitest.config.ts"
      provides: "Test runner config"
  key_links:
    - from: "src/lib/db.ts"
      to: "@neondatabase/serverless"
      via: "neon() (http) + Pool (websocket)"
      pattern: "@neondatabase/serverless"
    - from: "drizzle.config.ts"
      to: "src/db/schema.ts"
      via: "schema path"
      pattern: "src/db/schema"
---

<objective>
Scaffold the Next.js application and establish the data foundation the entire product builds on: a Next.js 16 App Router project, dual Neon Drizzle drivers, the complete 7-table schema as an applied migration, and the vitest test runner. This is the Walking Skeleton substrate — it ends with a real write + read proven against the live Neon database.

Purpose: Every later slice (auth, projects, tickets, board, GitHub sync) queries this schema and these driver instances. Defining the full schema now (D-06) means later phases add features against an existing schema instead of churning migrations.
Output: Runnable Next.js app, src/db/schema.ts (7 tables), src/lib/db.ts (dual drivers), applied Drizzle migration in Neon, vitest config + passing DB connectivity test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-auth-database-foundation/01-CONTEXT.md
@.planning/phases/01-auth-database-foundation/01-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- The schema contracts this plan defines (consumed by all later plans). Source: RESEARCH.md Code Examples. -->
Better Auth core tables (table names are SINGULAR — required by Better Auth defaults):
  user(id text pk, name text, email text unique, emailVerified boolean, image text, createdAt ts, updatedAt ts)
  session(id text pk, expiresAt ts, token text unique, createdAt ts, updatedAt ts, ipAddress text, userAgent text, userId text -> user.id cascade)
  account(id text pk, accountId text, providerId text, userId text -> user.id cascade, accessToken text, refreshToken text, idToken text, accessTokenExpiresAt ts, refreshTokenExpiresAt ts, scope text, password text, createdAt ts, updatedAt ts)
  verification(id text pk, identifier text, value text, expiresAt ts, createdAt ts, updatedAt ts)

App-domain tables (D-06/D-07/D-08):
  project(id text pk, name text, ticketKey text unique, ticketCounter integer default 0, ownerId text -> user.id cascade, githubRepo text nullable, createdAt ts, updatedAt ts)
  project_member(id text pk, projectId text -> project.id cascade, userId text -> user.id cascade, role text enum[owner,member], createdAt ts)
  invitation(id text pk, projectId text -> project.id cascade, token text unique, expiresAt ts, createdAt ts)
  ticket(id text pk, projectId text -> project.id cascade, ticketNumber integer, title text, description text nullable, status ticket_status default 'backlog', assigneeId text -> user.id set null, githubBranch text nullable, createdAt ts, updatedAt ts) + unique(projectId, ticketNumber)
  ticket_status enum: backlog, todo, in_progress, in_review, done
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 16 + install pinned stack + vitest</name>
  <read_first>
    - CLAUDE.md (full stack contract: pinned versions, "What NOT to Use" table)
    - .planning/phases/01-auth-database-foundation/01-RESEARCH.md (Installation block, Standard Stack table, Environment Availability)
  </read_first>
  <files>package.json, tsconfig.json, next.config.ts, .gitignore, .env.example, vitest.config.ts, src/tests/setup.ts</files>
  <action>
    Run `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --no-turbopack --import-alias "@/*"` in the existing (non-empty) repo; accept overwrite/merge with the existing README.md and CLAUDE.md (do not delete them). This installs Next.js 16.2.6 + React 19.2.7 + TypeScript + Tailwind v4 — note in the SUMMARY that 16.2.6 was chosen over CLAUDE.md's stated 15.x per the orchestrator's explicit decision (key decision 1); all other CLAUDE.md pins remain authoritative.
    Install runtime deps with EXACT pins from RESEARCH: `npm install better-auth drizzle-orm @neondatabase/serverless@^0.10.4 bcryptjs`. Install dev deps: `npm install --save-dev drizzle-kit @types/bcryptjs vitest @vitejs/plugin-react`.
    After install, verify package.json shows `@neondatabase/serverless` at `^0.10.4` (NOT ^1.x — pitfall 2 / drizzle-orm#5208). If npm resolved a 1.x, force `npm install @neondatabase/serverless@0.10.4`.
    Create vitest.config.ts with the @vitejs/plugin-react plugin, a `node` test environment, `globals: true`, and `setupFiles: ['src/tests/setup.ts']`. Add npm scripts: `"test": "vitest run"`, `"test:watch": "vitest"`.
    Create src/tests/setup.ts that loads .env.local (via dotenv or Next's loadEnvConfig) so DATABASE_URL is available to tests.
    Create .env.example listing (with placeholder values, NEVER real secrets): DATABASE_URL, BETTER_AUTH_SECRET, BETTER_AUTH_URL, GITHUB_CLIENT_ID, GITHUB_CLIENT_SECRET, NEXT_PUBLIC_APP_URL. Confirm .gitignore includes .env*.local and node_modules.
  </action>
  <verify>
    <automated>npm run build && npm run lint && npx vitest --version</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 and `npm run lint` exits 0
    - `grep '"@neondatabase/serverless"' package.json` shows a `^0.10` spec, NOT `^1` or `1.`
    - .env.example exists and contains the 6 env var names above with NO real secret values (placeholders only)
    - vitest.config.ts exists; `npx vitest --version` prints a version
  </acceptance_criteria>
  <done>Next.js app scaffolds, builds, lints; pinned deps installed at correct versions; vitest configured; .env.example documents required vars without secrets.</done>
</task>

<task type="auto">
  <name>Task 2: Define full 7-table schema + dual Neon drivers + drizzle config</name>
  <read_first>
    - .planning/phases/01-auth-database-foundation/01-RESEARCH.md (Code Examples: Better Auth core tables, App domain tables, drizzle.config.ts, Pattern 1 dual driver)
    - .planning/phases/01-auth-database-foundation/01-CONTEXT.md (D-06, D-07, D-08)
    - CLAUDE.md ("What NOT to Use": neon-http for Better Auth throws no-transactions)
    - package.json (confirm installed driver/orm versions)
  </read_first>
  <files>src/db/schema.ts, src/lib/db.ts, drizzle.config.ts</files>
  <action>
    Generate the Better Auth core tables first: run `npx @better-auth/cli generate --output src/db/schema.ts` ONLY if a minimal src/lib/auth.ts stub exists; if it does not yet exist, hand-write the 4 core tables (user, session, account, verification) directly per the RESEARCH "Better Auth core tables" example (table names singular: 'user','session','account','verification'). Then hand-write the 4 app-domain tables in the SAME src/db/schema.ts file per the RESEARCH "App domain tables" example: ticketStatusEnum via pgEnum('ticket_status', ['backlog','todo','in_progress','in_review','done']) (D-07), project, project_member, invitation, ticket. Apply the unique constraint `unique().on(table.projectId, table.ticketNumber)` on ticket (D-08). Use the exact column names, FK references, and onDelete behaviors from the <interfaces> block.
    Create src/lib/db.ts per Pattern 1: import neon and Pool from '@neondatabase/serverless'; `db` = drizzle from 'drizzle-orm/neon-http' (app queries); `authDb` = drizzle from 'drizzle-orm/neon-serverless' using a Pool (Better Auth transactional writes). Both pass `{ schema }`. authDb MUST use neon-serverless — neon-http throws "No transactions support" with Better Auth (pitfall 1).
    Create drizzle.config.ts via defineConfig: schema './src/db/schema.ts', out './src/db/migrations', dialect 'postgresql', dbCredentials.url process.env.DATABASE_URL.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "pgTable" src/db/schema.ts && grep -q "neon-serverless" src/lib/db.ts && grep -q "neon-http" src/lib/db.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - src/db/schema.ts defines 8 pgTable calls (4 Better Auth core + 4 app) — `grep -c "pgTable(" src/db/schema.ts` returns 8
    - src/db/schema.ts contains `pgEnum('ticket_status'` with exactly the 5 D-07 statuses
    - src/db/schema.ts contains a `unique()` constraint referencing projectId and ticketNumber (D-08)
    - src/lib/db.ts exports both `db` (neon-http) and `authDb` (neon-serverless)
    - drizzle.config.ts references './src/db/schema.ts' and './src/db/migrations'
  </acceptance_criteria>
  <done>All 7 logical tables (8 pgTable defs incl. verification) defined in one schema file with the locked enum and unique constraint; dual drivers exported; drizzle-kit config valid; types compile.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: [BLOCKING] Generate + apply Drizzle migration to Neon</name>
  <read_first>
    - .planning/phases/01-auth-database-foundation/01-RESEARCH.md (Environment Availability: DATABASE_URL required; migration tooling)
    - drizzle.config.ts
    - src/db/schema.ts
  </read_first>
  <files>src/db/migrations/, src/tests/db.test.ts</files>
  <what-built>
    A combined Drizzle migration for all 7 tables, generated from src/db/schema.ts and applied to the live Neon database. Plus a vitest DB connectivity test (src/tests/db.test.ts) that proves a real write + read against Neon (e.g., insert a throwaway row into `user` then select it then delete it, OR insert+select+delete a `project` row).
    This task is BLOCKING and requires a live DATABASE_URL in .env.local that Claude cannot self-provision. The migration is mandatory: build and tsc pass WITHOUT it (types derive from schema, not the live DB), so verification would be a false positive without an applied migration.
  </what-built>
  <how-to-verify>
    1. Ensure .env.local has a valid Neon DATABASE_URL (from the Neon project you created).
    2. Run `npx drizzle-kit generate` — confirm SQL files appear under src/db/migrations/.
    3. Run `npx drizzle-kit migrate` — confirm it reports applied migrations with no errors.
    4. Run `npm test` — the src/tests/db.test.ts connectivity test must pass (real write + read against Neon).
    5. In the Neon Console SQL editor, run: `SELECT table_name FROM information_schema.tables WHERE table_schema='public';` and confirm all 8 tables appear (user, session, account, verification, project, project_member, invitation, ticket).
    Report the table list or paste any error.
  </how-to-verify>
  <action>
    With a valid DATABASE_URL in .env.local: run `npx drizzle-kit generate` to emit SQL migration files under src/db/migrations/, then `npx drizzle-kit migrate` to apply them to the live Neon DB (generate+migrate produces versioned SQL — success criterion 5 requires migrations, not `push`). Then create src/tests/db.test.ts: insert a throwaway row (e.g. into `user` or `project`), select it back, assert equality, and delete it in cleanup — proving a real write+read against Neon. Run `npm test` to confirm it passes. This task is autonomous:false because applying the migration needs live Neon credentials Claude cannot self-provision.
  </action>
  <verify>
    <automated>ls src/db/migrations/*.sql >/dev/null 2>&1 && echo "migration files present"</automated>
  </verify>
  <acceptance_criteria>
    - SQL migration file(s) exist under src/db/migrations/
    - `npx drizzle-kit migrate` completes without error against the live Neon DB
    - src/tests/db.test.ts performs an insert + select (+ cleanup) and `npm test` passes
    - Neon information_schema lists all 8 tables
  </acceptance_criteria>
  <done>Versioned SQL migration applied to Neon; all 8 tables present; src/tests/db.test.ts proves a real write+read and passes via `npm test`.</done>
  <resume-signal>Type "approved" once all 8 tables are confirmed in Neon and the DB test passes, or paste the error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine -> Neon | DATABASE_URL credential crosses to a cloud DB; must not be committed |
| repo -> git history | .env files must never enter version control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information disclosure | DATABASE_URL / secrets in repo | mitigate | .env.example carries placeholders only; .gitignore excludes .env*.local; verify no real secret strings committed |
| T-01-02 | Tampering | SQL injection via app queries | mitigate | All queries go through Drizzle parameterized query builder; no raw string concatenation |
| T-01-03 | Denial of service | neon-http used for Better Auth transactions | mitigate | authDb uses neon-serverless (WebSocket) — neon-http throws "No transactions support" (pitfall 1) |
| T-01-SC | Tampering | npm package installs (better-auth, drizzle-orm, @neondatabase/serverless, bcryptjs) | mitigate | All packages are [ASSUMED] per RESEARCH Package Legitimacy Audit (official repos, official docs). slopcheck was unavailable; see Task gate note below. |
</threat_model>

<verification>
- `npm run build` and `npm run lint` exit 0
- `npx tsc --noEmit` exits 0
- `@neondatabase/serverless` pinned to ^0.10.x in package.json
- All 8 tables present in Neon via applied migration
- DB connectivity test passes (real write + read)
- No real secrets in .env.example or git
</verification>

<success_criteria>
Runnable Next.js 16 app; full 7-table schema applied to Neon as a versioned migration (success criterion 5); dual Neon drivers wired; vitest operational with a passing live-DB connectivity test. Foundation ready for the auth slices in Wave 2.
</success_criteria>

<output>
Create `.planning/phases/01-auth-database-foundation/01-01-SUMMARY.md` when done.
</output>
