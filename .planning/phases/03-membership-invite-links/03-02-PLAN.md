---
phase: 03-membership-invite-links
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/app/actions/invite.ts
  - src/components/invite-panel.tsx
  - src/app/dashboard/projects/[id]/members/page.tsx
  - src/app/dashboard/projects/[id]/page.tsx
autonomous: true
requirements: [MEM-01, MEM-04]
must_haves:
  truths:
    - "Project owner can generate a shareable invite URL and copy it without sending an email"
    - "Regenerating the link replaces the token and invalidates the previous link"
    - "Any member can open the members page and see the roster of members with role badges"
    - "Non-members hitting the members page get notFound() before any DB read"
    - "Non-owners cannot generate/regenerate a link (server rejects, not just hidden UI)"
  artifacts:
    - path: "src/app/actions/invite.ts"
      provides: "generateInviteLink owner-only Server Action (one active invitation row per project)"
      contains: "export async function generateInviteLink"
    - path: "src/app/dashboard/projects/[id]/members/page.tsx"
      provides: "members page: roster (all members) + owner-only invite panel"
      contains: "requireProjectMember"
    - path: "src/components/invite-panel.tsx"
      provides: "client invite panel: Copy + Regenerate/Generate with loading state"
      contains: "navigator.clipboard"
  key_links:
    - from: "src/components/invite-panel.tsx"
      to: "generateInviteLink"
      via: "useActionState"
      pattern: "useActionState"
    - from: "src/app/dashboard/projects/[id]/members/page.tsx"
      to: "requireProjectOwner"
      via: "role check for owner-only panel render"
      pattern: "membership.role"
    - from: "src/app/dashboard/projects/[id]/page.tsx"
      to: "/dashboard/projects/[id]/members"
      via: "header Members link"
      pattern: "/members"
---

<objective>
Vertical slice: an owner generates and copies a reusable invite link from a new
members page, and any member can view the roster. Cuts UI → Server Action → DB:
the members page (server component, member-guarded) renders the roster + an
owner-only invite panel; the panel (client) calls `generateInviteLink` and offers
Copy + Regenerate; the project detail header gains a Members link.

Purpose: MEM-01 (generate shareable link, copy without email) and MEM-04 (view
member list). One reusable link per project (D-22), 30-day expiry (D-24),
owner-only via requireProjectOwner (D-25).
Output: invite.ts action, invite-panel.tsx, members/page.tsx, header link.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-membership-invite-links/03-CONTEXT.md
@.planning/phases/03-membership-invite-links/03-PATTERNS.md
@.planning/phases/03-membership-invite-links/03-UI-SPEC.md
@CLAUDE.md

<interfaces>
From src/lib/project-access.ts (after Plan 01):
```typescript
export async function requireProjectMember(projectId, userId): Promise<{projectId; userId; role: 'owner'|'member'}>;
export async function requireProjectOwner(projectId, userId): Promise<ProjectMembership>; // throws ProjectAccessError for non-owners
export class ProjectAccessError extends Error {}
```

From src/db/schema.ts:
```typescript
export const invitations = pgTable('invitation', {
  id: text('id').primaryKey(),
  projectId: text('project_id').notNull().references(...),
  token: text('token').notNull().unique(),
  expiresAt: timestamp('expires_at').notNull(),   // NOT NULL — a value is required
  createdAt: timestamp('created_at').notNull(),
});
export const projects = pgTable('project', { id, name, ticketKey, ownerId, ... });
export const projectMembers = pgTable('project_member', { id, projectId, userId, role, createdAt });
export const users = pgTable('user', { id, name, email, ... });
```

createProject Server Action pattern to copy (src/app/actions/projects.ts):
- `const session = await auth.api.getSession({ headers: await headers() })`; guard `!session?.user`
- `crypto.randomUUID()` ids; `const now = new Date()`
- `await db.batch([...])` (neon-http has NO interactive transactions — never db.transaction)
- 23505 mapping: `(err as {code?})?.code ?? (err as {cause?:{code?}})?.cause?.code`; re-throw other codes
- `revalidatePath(path); return { success: true }`

create-project-dialog.tsx client pattern: `useActionState(action, initialState)` → `[state, action, isPending]`; Loader2 spinner while isPending.

env: `process.env.NEXT_PUBLIC_APP_URL` → invite URL = `${NEXT_PUBLIC_APP_URL}/invite/${token}`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: generateInviteLink owner-only Server Action (MEM-01)</name>
  <files>src/app/actions/invite.ts</files>
  <read_first>
    - src/app/actions/projects.ts (the whole file — copy session resolution, db.batch, 23505 handling, revalidatePath, the re-throw discipline)
    - src/lib/project-access.ts (requireProjectOwner, ProjectAccessError — from Plan 01)
    - src/db/schema.ts (invitations table: token unique, expiresAt NOT NULL)
    - .planning/phases/03-membership-invite-links/03-CONTEXT.md (D-22 one reusable link, D-23 high-entropy URL-safe token, D-24 30-day expiry, D-25 owner-only)
    - .planning/phases/03-membership-invite-links/03-PATTERNS.md (section "src/app/actions/invite.ts")
  </read_first>
  <action>
    Create `'use server'` file exporting type `GenerateInviteState = { errors?: { server?: string }; success?: boolean; url?: string }`
    and `export async function generateInviteLink(prevState, formData): Promise<GenerateInviteState>`.
    Read `projectId` from formData. Resolve session via
    `auth.api.getSession({ headers: await headers() })`; return
    `{ errors: { server: 'Not authenticated' } }` if no user. Call
    `requireProjectOwner(projectId, session.user.id)` inside try/catch — on
    ProjectAccessError return `{ errors: { server: 'Forbidden' } }`, re-throw
    otherwise. Generate a high-entropy URL-safe token with
    `crypto.randomBytes(32).toString('base64url')` (import `randomBytes` from
    'node:crypto'); `expiresAt = new Date(Date.now() + 30*24*60*60*1000)`;
    `id = crypto.randomUUID()`; `now = new Date()`. Enforce one active row per
    project via `db.batch([ db.delete(invitations).where(eq(invitations.projectId, projectId)), db.insert(invitations).values({ id, projectId, token, expiresAt, createdAt: now }) ])`
    (delete-then-insert; NOT db.transaction — neon-http). Wrap in try/catch mapping
    SQLSTATE 23505 (token collision, astronomically unlikely) to
    `{ errors: { server: 'Could not generate link, try again.' } }`; re-throw other
    codes. On success `revalidatePath(\`/dashboard/projects/${projectId}/members\`)`
    and return `{ success: true, url: \`${process.env.NEXT_PUBLIC_APP_URL}/invite/${token}\` }`.
  </action>
  <verify>
    <automated>grep -q "export async function generateInviteLink" src/app/actions/invite.ts && grep -q "db.batch" src/app/actions/invite.ts && ! grep -q "db.transaction" src/app/actions/invite.ts && npx vitest run src/tests/membership.test.ts -t generateInviteLink 2>&1 | grep -qiE "pass|✓" && echo GENERATE_OK</automated>
  </verify>
  <acceptance_criteria>
    - src/app/actions/invite.ts contains `export async function generateInviteLink` guarded by requireProjectOwner
    - Uses db.batch (delete-then-insert), never db.transaction
    - Token from node:crypto randomBytes(32).toString('base64url'); expiresAt = +30 days
    - Returns url = `${NEXT_PUBLIC_APP_URL}/invite/${token}` on success; revalidates the members path
    - The MEM-01 generateInviteLink tests in membership.test.ts pass (one active row per project; regenerate replaces token)
  </acceptance_criteria>
  <done>An owner can generate/replace exactly one invitation row per project; non-owners are rejected server-side.</done>
</task>

<task type="auto">
  <name>Task 2: Members page with roster + owner-only invite panel (MEM-04)</name>
  <files>src/app/dashboard/projects/[id]/members/page.tsx, src/app/dashboard/projects/[id]/page.tsx</files>
  <read_first>
    - src/app/dashboard/projects/[id]/page.tsx (the whole file — copy params-as-Promise, session→redirect('/login'), requireProjectMember→notFound() guard, header + main layout shell; this file is ALSO modified to add the Members link)
    - src/components/project-list.tsx (role badge pattern: owner → Badge secondary, member → Badge outline)
    - src/lib/project-access.ts (requireProjectMember returns role; ProjectAccessError)
    - src/db/schema.ts (projectMembers, users, invitations, projects)
    - .planning/phases/03-membership-invite-links/03-UI-SPEC.md (Page Inventory → /dashboard/projects/[id]/members layout, copywriting contract)
    - .planning/phases/03-membership-invite-links/03-PATTERNS.md (sections for members/page.tsx and the detail-page header modify)
  </read_first>
  <action>
    Create `src/app/dashboard/projects/[id]/members/page.tsx` as an async server
    component: `params: Promise<{ id: string }>`; resolve session, `redirect('/login')`
    if none; `const membership = await requireProjectMember(id, session.user.id)` in
    try/catch mapping ProjectAccessError → `notFound()` (re-throw others) BEFORE any
    project/member SELECT. Then load the project (name) and the full member roster
    joining `projectMembers` to `users`. The roster SELECT MUST select three columns —
    `select({ userId: projectMembers.userId, name: users.name, role: projectMembers.role })`
    from projectMembers innerJoin users on `eq(projectMembers.userId, users.id)` where
    `eq(projectMembers.projectId, id)` — so each roster row has shape
    `{ userId: string; name: string; role: 'owner'|'member' }`. This `userId` column is
    REQUIRED downstream: Plan 04 Task 3 wires `<MemberList members={roster} ...>` and
    calls `removeMember(projectId, row.userId)`; omitting `userId` breaks 03-04 at tsc
    and runtime. Also load the current invitation row (if any) via
    `db.select().from(invitations).where(eq(invitations.projectId, id)).limit(1)`.
    Render the layout from UI-SPEC: shared h-14 header, `container mx-auto max-w-4xl px-6 py-8`,
    a back link to `/dashboard/projects/${id}`, `<h1>Members</h1>`. When
    `membership.role === 'owner'`, render `<InvitePanel projectId={id} inviteUrl={existingUrl ?? null} />`
    (compute existingUrl from the invitation token + NEXT_PUBLIC_APP_URL) and a
    `<Separator className="my-6" />`. Always render the roster section
    (`<h2>Team members</h2>`) using the role-badge pattern (owner → secondary,
    member → outline) for every member — this is MEM-04 and is visible to any member.
    Then MODIFY `src/app/dashboard/projects/[id]/page.tsx`: in the existing header,
    wrap the email span and a new `<Link href={\`/dashboard/projects/${id}/members\`}>Members</Link>`
    in a `<div className="flex items-center gap-4">`, keeping `justify-between`.
    `Link` and `id` are already in scope — add no imports there.
  </action>
  <verify>
    <automated>test -f "src/app/dashboard/projects/[id]/members/page.tsx" && grep -q "requireProjectMember" "src/app/dashboard/projects/[id]/members/page.tsx" && grep -q "Team members" "src/app/dashboard/projects/[id]/members/page.tsx" && grep -q "projectMembers.userId" "src/app/dashboard/projects/[id]/members/page.tsx" && grep -q "/members" "src/app/dashboard/projects/[id]/page.tsx" && npx tsc --noEmit 2>&1 | grep -v '^#' | grep -cE "members/page|projects/\[id\]/page" | grep -qx 0 && echo MEMBERS_PAGE_OK</automated>
  </verify>
  <acceptance_criteria>
    - members/page.tsx calls requireProjectMember and maps ProjectAccessError to notFound() before any roster SELECT
    - The roster SELECT selects `projectMembers.userId`, `users.name`, and `projectMembers.role`; each roster row has shape `{ userId: string; name: string; role: 'owner'|'member' }` (userId REQUIRED for Plan 04 wiring)
    - Roster lists every member's name + role Badge (owner secondary / member outline), visible to any member (MEM-04)
    - Invite panel + Separator render only when membership.role === 'owner'
    - Detail page header contains a working Members link to /dashboard/projects/[id]/members
    - tsc clean for both files
  </acceptance_criteria>
  <done>Members can view the roster (rows carry userId, name, role); owners additionally see the invite panel; detail header links to it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: InvitePanel client component — Copy + Regenerate (MEM-01)</name>
  <files>src/components/invite-panel.tsx</files>
  <behavior>
    - Renders a read-only Input with the invite URL when inviteUrl prop is non-null
    - When inviteUrl is null, renders a primary "Generate invite link" button instead of the URL field
    - Copy button calls navigator.clipboard.writeText and flips label to "Copied!" for ~2000ms
    - Regenerate/Generate button shows Loader2 spinner while the generateInviteLink action is pending
    - On clipboard failure, falls back to selecting the input contents
  </behavior>
  <read_first>
    - src/components/create-project-dialog.tsx (the whole file — useActionState wiring, isPending → Loader2 spinner pattern, Button usage)
    - src/components/ui/input.tsx, src/components/ui/label.tsx, src/components/ui/card.tsx, src/components/ui/button.tsx (props)
    - .planning/phases/03-membership-invite-links/03-UI-SPEC.md (Interaction Contracts: Copy / Regenerate / Generate; copywriting; helper text "Anyone with this link can join as a member. The link expires in 30 days.")
    - .planning/phases/03-membership-invite-links/03-PATTERNS.md (section "src/components/invite-panel.tsx")
  </read_first>
  <action>
    Create `'use client'` component `InvitePanel({ projectId, inviteUrl }: { projectId: string; inviteUrl: string | null })`.
    Use `useActionState(generateInviteLink, { } as GenerateInviteState)` →
    `[state, formAction, isPending]`. Render inside `<Card><CardContent className="p-4">`.
    A `<form action={formAction}>` carries a hidden `<input type="hidden" name="projectId" value={projectId} />`.
    When `inviteUrl` is present: `<Label htmlFor="invite-url">Invite link</Label>`, a
    read-only `<Input id="invite-url" readOnly value={inviteUrl} className="font-mono text-sm" ref={inputRef} />`,
    a `Copy` button (variant="outline", `Copy` lucide icon) using local `useState` copied
    flag + `navigator.clipboard.writeText(inviteUrl)` (catch → `inputRef.current?.select()`),
    label toggling "Copy"→"Copied!" for 2000ms via setTimeout, and a `Regenerate`
    submit button (variant="outline", `RefreshCw` icon, Loader2 + disabled while isPending).
    When `inviteUrl` is null: render only a primary submit button "Generate invite link"
    (Loader2 while isPending). Show the helper text and, if `state.errors?.server`,
    a `<p className="text-sm text-destructive">`. Do NOT hardcode NEXT_PUBLIC_APP_URL —
    the URL arrives as a prop from the server component (and via state.url after submit
    when present).
  </action>
  <verify>
    <automated>grep -q "navigator.clipboard" src/components/invite-panel.tsx && grep -q "useActionState" src/components/invite-panel.tsx && grep -q "Generate invite link" src/components/invite-panel.tsx && npx tsc --noEmit 2>&1 | grep -v '^#' | grep -c "invite-panel" | grep -qx 0 && echo PANEL_OK</automated>
  </verify>
  <acceptance_criteria>
    - src/components/invite-panel.tsx is a 'use client' component taking { projectId, inviteUrl }
    - Renders read-only URL Input + Copy + Regenerate when inviteUrl is set; "Generate invite link" button when null
    - Copy uses navigator.clipboard with select() fallback and "Copied!" feedback
    - Regenerate/Generate uses useActionState(generateInviteLink) with Loader2 pending state
    - tsc clean for invite-panel.tsx
  </acceptance_criteria>
  <done>Owner can generate, copy, and regenerate the invite link from the members page UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → generateInviteLink action | projectId from FormData is untrusted; caller may not be the owner |
| client → /members page | id from URL is untrusted; caller may be a non-member |
| token in invite URL | the token IS the secret (capability URL) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Elevation of Privilege | generateInviteLink | mitigate | requireProjectOwner(projectId, session.user.id) before any write; ProjectAccessError → { errors: { server: 'Forbidden' } }. UI gating is cosmetic only (D-25). |
| T-03-05 | Information Disclosure | invite token | mitigate | Token = randomBytes(32).base64url (256-bit, URL-safe, unguessable) per D-23; possession is the only join capability. |
| T-03-06 | Information Disclosure | /members page IDOR | mitigate | requireProjectMember → notFound() before any roster/invitation SELECT; non-members cannot enumerate or read project internals (D-15/D-31). |
| T-03-07 | Repudiation/Tampering | stale link reuse | mitigate | Regenerate replaces the single invitation row (delete-then-insert), invalidating the old token (D-22). |
| T-03-SC | Tampering | shadcn CLI install | accept | No package-manager installs in this plan (Plan 04 installs alert-dialog from the official shadcn registry). |
</threat_model>

<verification>
- `npx vitest run src/tests/membership.test.ts -t generateInviteLink` GREEN.
- `npx tsc --noEmit` clean.
- Manual (human-verify deferred to phase end): owner sees panel, copies link; member sees roster but no panel.
</verification>

<success_criteria>
- Owner generates a shareable invite URL and copies it with no email (MEM-01).
- Regenerate invalidates the previous link (one active row per project).
- Any member can view the roster with role badges (MEM-04); non-members get notFound().
</success_criteria>

<output>
Create `.planning/phases/03-membership-invite-links/03-02-SUMMARY.md` when done.
</output>
