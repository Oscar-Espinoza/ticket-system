You are agent **B7 cycles** in a multi-agent build of Linear-parity features for a Next.js 16 + Drizzle + Neon ticket system at /home/oscar/Projects/ticket-system (branch feat/linear-parity). About 11 other Wave B agents work IN PARALLEL in the same working tree, each owning different files. Wave A already built the foundation (data layer, workflow states, pickers, slots, navigation, settings scaffolds, stub pages).

## Read first
- `.planning/features/00-MASTER-PLAN.md` (naming, contracts, ownership — authoritative) and the Wave A plan docs in `.planning/features/A1-*.md`, `A2-*.md`
- `.planning/features/CONTRACTS.md` (exact exported APIs from Wave A — use these, don't reinvent them)
- `AGENTS.md`: this is **Next.js 16** — APIs differ from your training data. Check `node_modules/next/dist/docs/` whenever unsure (params are Promises, `after` from `next/server`, route handlers, metadata, caching).
- `CLAUDE.md` (stack notes) and `src/db/schema.ts` (already migrated — every table you need exists).

## Rules
1. **Plan doc first**: before implementing each feature, write `.planning/features/B7-<slug>.md` (goal, UX, data/actions, files, edge cases — concise, ~20-40 lines). One doc per checklist feature.
2. **Strict file ownership** (shared working tree!): edit only the files listed under "You own" plus new files you create inside your own directories. Every slot/stub file assigned to you is yours to overwrite. Never edit another agent's files, even to fix a type error — note it under "Integration requests" in your final report instead. Frozen hosts (don't edit): `src/components/issue-detail/issue-detail.tsx`, `src/components/issue-detail/issue-detail-pane.tsx`, `src/app/dashboard/projects/[id]/layout.tsx`, `src/components/app-shell/shell-frame.tsx`, `src/lib/issue-model.ts`, `src/lib/tickets.ts`, `src/lib/events.ts`, `src/lib/action-auth.ts`, `src/lib/project-data.ts`, `src/app/actions/tickets.ts`, `src/db/schema.ts` — unless listed under "You own".
3. Don't run drizzle-kit, don't write to the database, don't install packages (ask via Integration requests if you truly need one — prefer the platform), no git, no `next build` / `next dev`, no new tests. If you need a schema change, STOP that part and report it.
4. **Verify** before finishing: `npx tsc --noEmit --incremental false 2>&1 | grep -E 'cycles|triage|settings/planning|settings/automations|lib/automation|property-cycle|components/charts'` must show zero errors in your files (other agents are mid-flight; ignore errors in their files). `npx eslint <your files>` clean.
5. Style: match the codebase — sparse comments that explain *why*, shadcn components in `src/components/ui` (radix-nova; Popover, Command, DropdownMenu, ContextMenu, Dialog, AlertDialog, Sheet, Tooltip, HoverCard, Tabs, Switch, Select, Calendar, Checkbox, RadioGroup, ToggleGroup, Table, ScrollArea, Progress, Collapsible), domain glyphs in `src/components/ui-icons` (StatusIcon/StateIcon, PriorityIcon, LabelChip, Avatar, EmptyState, Skeleton), the reusable pickers in `src/components/issue-pickers/*`, lucide-react, Tailwind v4 tokens, sonner toasts, hotkey registry `src/lib/hotkeys.ts`, palette registry `src/lib/palette-commands.ts`. Linear look & feel: dense, quiet, keyboard-first, good empty states and loading states.
6. **Security**: every server action / route handler authorizes via `authorizeProjectAction` (or the project-access helpers) before reading or writing project data, and validates that every referenced id belongs to that project. Public endpoints (webhooks, API, intake) authenticate with their own secret/token and never leak other projects' data.
7. Mutations that change issues go through the existing actions in `src/app/actions/tickets.ts` (or your own actions that call `emitIssueEvent`) so activity history, notifications, Slack and webhooks all see them.
8. Keep the free-tier constraints: no paid services, no cron beyond lazy/throttled work, no websockets.

## Final report (your last message)
Features done (with plan doc paths); files created/modified; integration requests (exact file + exact change needed) for files you don't own; env vars added; anything unfinished or deviations.

## Your features
Cycles / sprints · Cycle burndown / velocity charts · Triage · Backlog auto-archive / auto-close

## You own
- `src/app/dashboard/projects/[id]/cycles/**`, `.../triage/**`, `.../settings/planning/**`, `.../settings/automations/**` (stubs — overwrite)
- `src/lib/automation.ts` (stub — implement), `src/lib/cycles.ts`, `src/lib/triage.ts` (new)
- `src/app/actions/cycles.ts`, `src/app/actions/triage.ts`, `src/app/actions/planning-settings.ts` (new)
- `src/components/cycles/**`, `src/components/triage/**`, `src/components/charts/**` (new — small hand-rolled SVG chart primitives: line/area, bar; accessible, theme tokens)
- `src/components/issue-detail/slots/property-cycle.tsx`

## Build
- **Planning settings**: enable cycles, duration (1–8 weeks), start weekday, auto-create upcoming cycles (keep 2 ahead), auto-rollover unfinished issues; enable triage. Admin only.
- **Cycles**: list page (current with progress, upcoming, past with completion %), create/edit/complete cycle manually, cycle detail page rendering `IssuesView` with the cycle's issues and `createDefaults={{ cycleId }}` plus a header with dates, progress (scope, started, completed — counts and estimate points), burndown chart (remaining scope per day vs ideal line; scope from issues in the cycle, completion from `completedAt`, scope changes from `activity` rows where `cycleId` changed), and a sidebar breakdown by assignee / label. Velocity chart on the list page (completed points per past cycle, avg of last 3). Property-cycle slot: cycle picker (current/upcoming first) + hidden when cycles are disabled.
- **Triage**: triage page listing issues in the triage state (newest first) with a detail preview; actions Accept (choose state — default first backlog/unstarted — plus optional priority/assignee), Decline (move to "Canceled" with optional comment-less reason in activity), Mark duplicate (pick the original with an issue search; create a `duplicate` relation row + move to the "Duplicate" canceled state), keyboard `1` accept, `2` duplicate, `3` decline. Badge count for the Triage tab is optional. When triage is enabled, issues created by the intake form / API / non-members land in triage (the intake agent handles that by passing the triage stateId).
- **Automations** (`runProjectAutomations(projectId)`, already called from the project layout via `after()`): throttle with an atomic `UPDATE project SET automation_run_at = now() WHERE id = $1 AND (automation_run_at IS NULL OR automation_run_at < now() - interval '1 day') RETURNING id`; then: auto-archive issues closed (completed/canceled) more than `autoArchiveMonths` ago; auto-close issues in backlog/unstarted/triage states untouched (`updatedAt`) for `autoCloseMonths` (move to the first canceled state, emit an event with summary "auto-closed after N months of inactivity"); purge trash older than 30 days; cycles: auto-create upcoming cycles, complete ended cycles and roll unfinished issues into the next cycle when enabled. All writes via the issue service with `actor { userId: null }`. Settings → Automations page edits the two month values (off / 1 / 3 / 6 / 9 / 12).

## Notes from Wave A
- The estimate scale is already editable in Settings → General (A2) — don't duplicate it.
- Triage/Cycles tabs are always visible in the sidebar and project tabs; show a helpful empty state with an "Enable in settings" link when the feature is off.
