You are agent **C2 platform fixes** in a multi-agent build of Linear-parity features for a Next.js 16 + Drizzle + Neon ticket system at /home/oscar/Projects/ticket-system (branch feat/linear-parity). This is **Wave C** (integration). Two other Wave C agents work IN PARALLEL in the same working tree, each owning different files. Waves A and B already built every feature (see all `.planning/features/*.md` docs); Wave C wires the loose ends together.

## Read first
- `.planning/features/00-MASTER-PLAN.md` (naming, contracts, ownership — authoritative) and the all plan docs in `.planning/features/` relevant to the files you touch
- `.planning/features/CONTRACTS.md` (exact exported APIs from Wave A — use these, don't reinvent them)
- `AGENTS.md`: this is **Next.js 16** — APIs differ from your training data. Check `node_modules/next/dist/docs/` whenever unsure (params are Promises, `after` from `next/server`, route handlers, metadata, caching).
- `CLAUDE.md` (stack notes) and `src/db/schema.ts` (already migrated — every table you need exists).

## Rules
1. **Plan doc first**: before implementing each feature, write `.planning/features/C2-<slug>.md` (goal, UX, data/actions, files, edge cases — concise, ~20-40 lines). For integration fixes, one doc `C?-integration-fixes.md` listing each fix is enough.
2. **Strict file ownership** (shared working tree!): edit only the files listed under "You own" plus new files you create inside your own directories. Every slot/stub file assigned to you is yours to overwrite. Never edit another agent's files, even to fix a type error — note it under "Integration requests" in your final report instead. Everything not listed under "You own" is off-limits (especially `src/db/schema.ts` — the Wave C migration `0005` is already applied).
3. Don't run drizzle-kit, don't write to the database, don't install packages (ask via Integration requests if you truly need one — prefer the platform), no git, no `next build` / `next dev`, no new tests. If you need a schema change, STOP that part and report it.
4. **Verify** before finishing: `npx tsc --noEmit --incremental false 2>&1 | grep -E 'hotkeys|hotkey-overlay|issue-shortcuts|app/layout|app-shell|breadcrumb|routes\.ts|sidebar-favorites|next\.config|\(auth\)|env|email-templates|app-url|attachment|projects/\[id\]/layout|use-issue-mutations|issue-bin|CONTRACTS|issue-detail\.tsx'` must show zero errors in your files (other agents are mid-flight; ignore errors in their files). `npx eslint <your files>` clean.
5. Style: match the codebase — sparse comments that explain *why*, shadcn components in `src/components/ui` (radix-nova; Popover, Command, DropdownMenu, ContextMenu, Dialog, AlertDialog, Sheet, Tooltip, HoverCard, Tabs, Switch, Select, Calendar, Checkbox, RadioGroup, ToggleGroup, Table, ScrollArea, Progress, Collapsible), domain glyphs in `src/components/ui-icons` (StatusIcon/StateIcon, PriorityIcon, LabelChip, Avatar, EmptyState, Skeleton), the reusable pickers in `src/components/issue-pickers/*`, lucide-react, Tailwind v4 tokens, sonner toasts, hotkey registry `src/lib/hotkeys.ts`, palette registry `src/lib/palette-commands.ts`. Linear look & feel: dense, quiet, keyboard-first, good empty states and loading states.
6. **Security**: every server action / route handler authorizes via `authorizeProjectAction` (or the project-access helpers) before reading or writing project data, and validates that every referenced id belongs to that project. Public endpoints (webhooks, API, intake) authenticate with their own secret/token and never leak other projects' data.
7. Mutations that change issues go through the existing actions in `src/app/actions/tickets.ts` (or your own actions that call `emitIssueEvent`) so activity history, notifications, Slack and webhooks all see them.
8. Keep the free-tier constraints: no paid services, no cron beyond lazy/throttled work, no websockets.

## Final report (your last message)
Features done (with plan doc paths); files created/modified; integration requests (exact file + exact change needed) for files you don't own; env vars added; anything unfinished or deviations.

## Your task
Apply the queued cross-agent integration fixes below (each was requested by a Wave A/B agent in its final report). Write `.planning/features/C2-integration-fixes.md` first (one line per fix: what + files), then implement.

## You own (only for the listed fixes)
`src/lib/hotkeys.ts`, `src/components/hotkey-overlay.tsx`, `src/components/issues/slots/issue-shortcuts.tsx`, `src/app/layout.tsx`, `src/components/app-shell/{app-shell,breadcrumb,routes}.ts(x)`, `src/components/app-shell/slots/sidebar-favorites.tsx`, `next.config.ts`, `src/app/(auth)/**`, `env.template`, `.env.example`, `src/lib/notifications/email-templates.ts`, `src/lib/integrations/app-url.ts`, `src/components/issue-hierarchy/attachment-utils.ts`, `src/lib/attachments.ts`, `src/app/api/attachments/**`, `src/components/issue-hierarchy/**` (attachment cap copy only), `src/app/dashboard/projects/[id]/layout.tsx`, `src/components/issues/use-issue-mutations.ts`, `src/components/navigation/issue-bin.tsx`, `src/components/issue-detail/issue-detail.tsx`, `.planning/features/CONTRACTS.md`.

## Fixes
1. **Hotkeys — Shift support** (`src/lib/hotkeys.ts`): add `shift?: boolean` to `Hotkey`; for hotkeys that set `shift`, `matches()` requires `event.shiftKey === true`, and for letter hotkeys that don't set it, keep today's behaviour (match regardless) — make sure `?` still works (it is typed with Shift). `formatHotkey` renders `⇧` (Mac) / `Shift+` and maps `' '` / `'Space'` to "Space". Then in `issue-shortcuts.tsx` (B10's) replace the physical-key listener for ⇧D / ⇧A / ⌘⇧, with registry entries and remove the display-only duplicates; also fix B3's duplicate "Copy git branch name" overlay entry if it's registered twice (it lives in `src/components/issue-detail/slots/header-github.tsx` — NOT yours; if it needs a change, note it in your report instead). Update `hotkey-overlay.tsx` if needed.
2. **Global `TooltipProvider`** in `src/app/layout.tsx` (wrap the app; keep existing providers and B12's PWA additions). Components that wrap their own provider keep working.
3. **Density boot script**: move A2's inline density script from `AppShell` into the root layout `<head>` (before paint, `suppressHydrationWarning` where needed) so it covers all pages and avoids the client-navigation warning; keep `src/lib/density.ts` semantics.
4. **Navigation commands**: render `<NavigationCommands />` (from `@/components/navigation/navigation-commands`) in `app-shell.tsx` next to `ShellFrame` and remove it from `sidebar-favorites.tsx` (so palette commands survive a collapsed sidebar).
5. **Breadcrumb labels** for `/dashboard/workspaces/[slug]` (+ initiatives) and `/dashboard/people/[userId]`, `/dashboard/inbox`, `/dashboard/drafts`, etc. — verify every route in `src/app/dashboard/**` gets a sensible label.
6. **Service worker header** in `next.config.ts`: `{ source: '/sw.js', headers: [{ key: 'Cache-Control', value: 'no-cache, no-store, must-revalidate' }, { key: 'Service-Worker-Allowed', value: '/' }] }` (merge with existing config).
7. **Auth redirect**: login/signup forms and pages honour `?redirect=` (also `callbackURL` for social sign-in) only when it is a same-origin relative path starting with `/invite/` or `/dashboard` (reject `//`, schemes, backslashes); already-signed-in users visiting /login?redirect=… go there too. Invite pages already link to `/login?redirect=…` — check the exact param name they use (`src/app/invite/**`) and match it.
8. **Env**: add `GITHUB_WEBHOOK_BASE_URL=` (optional, commented) to `env.template` and `.env.example`, plus `RESEND_API_KEY` / `EMAIL_FROM` to `.env.example` if missing.
9. **One app-origin helper**: make `src/lib/notifications/email-templates.ts` use `src/lib/integrations/app-url.ts` instead of its own `appOrigin()` (keep behaviour: NEXT_PUBLIC_APP_URL → BETTER_AUTH_URL → VERCEL_PROJECT_PRODUCTION_URL → localhost).
10. **Attachment cap** 5 MB → 4 MB everywhere (Vercel Hobby rejects bodies > 4.5 MB): client check, server check, copy.
11. **Project favorite**: add `<FavoriteButton targetType="project" targetId={id} />` to the project header in `src/app/dashboard/projects/[id]/layout.tsx`.
12. **Unarchive**: add `unarchive(issue, onDone?)` to `useIssueMutations` (calls `unarchiveIssue`; B10's undo stack must get the inverse `archive`), keep the API backwards compatible, and use it in `src/components/navigation/issue-bin.tsx` for archived issues (Restore stays for trashed ones).
13. **Issue detail menu hints**: show `⌘.` and `⌘⇧,` shortcut hints (via `formatHotkey`) next to Copy ID / Copy link in `issue-detail.tsx`'s `…` menu.
14. **CONTRACTS.md**: update the filters/display section to B5's new shapes (`IssueFilters` fields, `normalizeIssueFilters`, `ME`/`NONE`, `IssueFiltersProvider`, `useIssueFilters`/`useSetIssueFilters`/`setIssueFilters`, `DisplayOptions.layout`, `normalizeDisplayOptions`, `useSavedView`, `useSubIssueCount`, `IssuesView` props) and add the B8 and B10 entries: B8 — `src/lib/epics.ts` (`getProjectEpics`, `getEpicDetail`), `src/lib/initiatives.ts`, `src/app/actions/{epics,initiatives}.ts`, `Timeline` (`src/components/roadmap/timeline.tsx`), `epicPath`, `initiativesPath`; B10 — `requestNewIssue`/`NewIssueSeed` (`src/components/productivity/new-issue-bus.ts`), `NewIssueDialog.fallback`, `pushUndo`/`runUndo`/`undoLast`/`undoToastAction` (`src/lib/undo.ts`), `inversePatch`, `PropertyChips`, `useTemplates`, `templateProps`. Read each file to get the names right.
