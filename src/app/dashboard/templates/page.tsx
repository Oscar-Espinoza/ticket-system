// Stub — owned by D4
import type { Metadata } from 'next';

import { ComingSoon } from '@/components/coming-soon';

export const metadata: Metadata = { title: 'Project templates' };

export default function Page() {
  return <ComingSoon title="Project templates" />;
}
