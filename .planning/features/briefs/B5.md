You are agent **B5 views engine** in a multi-agent build of Linear-parity features for a Next.js 16 + Drizzle + Neon ticket system at /home/oscar/Projects/ticket-system (branch feat/linear-parity). About 11 other Wave B agents work IN PARALLEL in the same working tree, each owning different files. Wave A already built the foundation (data layer, workflow states, pickers, slots, navigation, settings scaffolds, stub pages).

## Read first
- `.planning/features/00-MASTER-PLAN.md` (naming, contracts, ownership — authoritative) and the Wave A plan docs in `.planning/features/A1-*.md`, `A2-*.md`
- `.planning/features/CONTRACTS.md` (exact exported APIs from Wave A — use these, don't reinvent them)
- `AGENTS.md`: this is **Next.js 16** — APIs differ from your training data. Check `node_modules/next/dist/docs/` whenever unsure (params are Promises, `after` from `next/server`, route handlers, metadata, caching).
- `CLAUDE.md` (stack notes) and `src/db/schema.ts` (already migrated — every table you need exists).

## Rules
1. **Plan doc first**: before implementing each feature, write `.planning/features/B5-<slug>.md` (goal, UX, data/actions, files, edge cases — concise, ~20-40 lines). One doc per checklist feature.
2. **Strict file ownership** (shared working tree!): edit only the files listed under "You own" plus new files you create inside your own directories. Every slot/stub file assigned to you is yours to overwrite. Never edit another agent's files, even to fix a type error — note it under "Integration requests" in your final report instead. Frozen hosts (don't edit): `src/components/issue-detail/issue-detail.tsx`, `src/components/issue-detail/issue-detail-pane.tsx`, `src/app/dashboard/projects/[id]/layout.tsx`, `src/components/app-shell/shell-frame.tsx`, `src/lib/issue-model.ts`, `src/lib/tickets.ts`, `src/lib/events.ts`, `src/lib/action-auth.ts`, `src/lib/project-data.ts`, `src/app/actions/tickets.ts`, `src/db/schema.ts` — unless listed under "You own".
3. Don't run drizzle-kit, don't write to the database, don't install packages (ask via Integration requests if you truly need one — prefer the platform), no git, no `next build` / `next dev`, no new tests. If you need a schema change, STOP that part and report it.
4. **Verify** before finishing: `npx tsc --noEmit --incremental false 2>&1 | grep -E 'components/issues/|components/board/|components/views/|issue-grouping|issue-filtering|issue-model'` must show zero errors in your files (other agents are mid-flight; ignore errors in their files). `npx eslint <your files>` clean.
5. Style: match the codebase — sparse comments that explain *why*, shadcn components in `src/components/ui` (radix-nova; Popover, Command, DropdownMenu, ContextMenu, Dialog, AlertDialog, Sheet, Tooltip, HoverCard, Tabs, Switch, Select, Calendar, Checkbox, RadioGroup, ToggleGroup, Table, ScrollArea, Progress, Collapsible), domain glyphs in `src/components/ui-icons` (StatusIcon/StateIcon, PriorityIcon, LabelChip, Avatar, EmptyState, Skeleton), the reusable pickers in `src/components/issue-pickers/*`, lucide-react, Tailwind v4 tokens, sonner toasts, hotkey registry `src/lib/hotkeys.ts`, palette registry `src/lib/palette-commands.ts`. Linear look & feel: dense, quiet, keyboard-first, good empty states and loading states.
6. **Security**: every server action / route handler authorizes via `authorizeProjectAction` (or the project-access helpers) before reading or writing project data, and validates that every referenced id belongs to that project. Public endpoints (webhooks, API, intake) authenticate with their own secret/token and never leak other projects' data.
7. Mutations that change issues go through the existing actions in `src/app/actions/tickets.ts` (or your own actions that call `emitIssueEvent`) so activity history, notifications, Slack and webhooks all see them.
8. Keep the free-tier constraints: no paid services, no cron beyond lazy/throttled work, no websockets.

## Final report (your last message)
Features done (with plan doc paths); files created/modified; integration requests (exact file + exact change needed) for files you don't own; env vars added; anything unfinished or deviations.

## Your features
Filter by priority / label / date / creator · Group by (assignee, priority, label, etc.) · Sort options · Display options (visible properties) · Sub-grouping / swimlanes · Spreadsheet / table view · Calendar view · Manual sort order within a column

## You own
- `src/components/issues/issue-filters.tsx`, `display-options.tsx`, `issues-view.tsx`, `views.tsx`, `view-switcher.tsx`, `issue-list.tsx`, `issue-row.tsx`, `issue-status-menu.tsx`
- `src/components/board/**`, `src/components/views/**` (new: table, calendar)
- `src/lib/issue-grouping.ts`, `src/lib/issue-filtering.ts` (new), and the **filter section only** of `src/lib/issue-model.ts` (you may change `IssueFilters` and its parse/filter helpers; don't change other exported types)
- NOT yours: `new-issue-dialog.tsx` and `use-issue-mutations.ts` (B10), slot files under `src/components/issues/slots/` (B6/B10/Wave C — but you must render them)

## Build
- **Filters**: Linear-style "Filter" button (hotkey `f`) opening a two-level menu (property → values with search + checkboxes): status (states + by type), assignee (incl. unassigned, "me"), creator, priority, labels (any-of), cycle, epic, due date (overdue, today, this week, next week, no due date), created / updated (last 1/7/30/90 days), has sub-issues / is sub-issue; active filters render as removable chips; plus a quick text filter box (title/key). URL-serializable (keep `?state=…&assignee=…` style) AND JSON-serializable for saved views. Export from `src/lib/issue-filtering.ts`: `IssueFilters`, `EMPTY_FILTERS`, `filterIssues`, `filtersFromSearchParams`, `filtersToSearchParams`, `isFilterActive`; keep a `useIssueFilters()` hook (+ `setIssueFilters`) in `issue-filters.tsx`.
- **Display options** menu (hotkey `shift+v` or a "Display" button): layout (list/board/table/calendar), grouping (state, assignee, priority, label, cycle, epic, none), sub-grouping (same set; renders swimlanes on board, nested groups in list), ordering (manual, priority, created, updated, due date, title), show empty groups, show sub-issues, visible properties toggles. Persist per project in localStorage (try/catch), `?view=` URL keeps layout. `useDisplayOptions()` stays the single source.
- **Grouping** in `issue-grouping.ts`: implement all `GroupBy` kinds with correct `patch` so dropping on a board column / creating in a group applies it (assignee → `assigneeId`, priority, label → add that label, cycle, epic, none → null patch). Label grouping puts multi-label issues in each group; issues with no value go into a "No …" group.
- **Ordering + manual sort**: `orderBy: 'manual'` sorts by `sortOrder`; drag to reorder within a list group and within a board column (dnd-kit `@dnd-kit/react/sortable`), writing a midpoint `sortOrder` through `mutations.update`. Cross-column drop still applies the group patch.
- **Table view** (`components/views/table-view.tsx`): spreadsheet with sticky header, columns = visible properties, inline editing via the pickers in `src/components/issue-pickers/*`, sortable column headers (sets orderBy), row click opens the issue, keyboard row navigation.
- **Calendar view** (`components/views/calendar-view.tsx`): month grid by `dueDate` with prev/next/today, issue chips (state icon + key + title), drag a chip to another day to change `dueDate`, "No due date" sidebar list you can drag from, click opens the issue.
- **IssuesView contract** (other agents render it): props `issues`, optional `createDefaults?: IssuePatch` (merged into new issues and board-column creates — used by cycle/epic pages), `initialFilters?`, `initialDisplay?` (saved views), `savedView?: { id; name }` (shows the view name + "Save changes" affordance through the toolbar slot), `emptyState?`. It must render these slots: `ToolbarExtra` (`src/components/issues/slots/toolbar-extra.tsx`), `ListOverlay` (`list-overlay.tsx`), `BulkBar` (`bulk-bar.tsx`), `IssueShortcuts` (`issue-shortcuts.tsx` — props `{ issues, mutations, selectedIssue }`). Keep today's keyboard behaviour (j/k/s/Enter, board Space pick-up) and the detail pane.
- Row/card/table rendering must honour `display.properties` and `showSubIssues` (when off, hide issues whose parent is in the list and show a sub-issue count chip on parents).

## Notes from Wave A
- Wave A already added group-header "+" buttons in list view (they pass `group.patch`) and renders the slots in `IssuesView` — keep them.
- `DEFAULT_DISPLAY_OPTIONS.showEmptyGroups` is false for the list; the board always shows every column. Guests get disabled pickers (`useProjectPermission`).
- Filters currently use URL params `state` (comma ids) and `assignee`; keep backwards compatibility.
