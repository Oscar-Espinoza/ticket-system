You are agent **C1 bulk edit** in a multi-agent build of Linear-parity features for a Next.js 16 + Drizzle + Neon ticket system at /home/oscar/Projects/ticket-system (branch feat/linear-parity). This is **Wave C** (integration). Two other Wave C agents work IN PARALLEL in the same working tree, each owning different files. Waves A and B already built every feature (see all `.planning/features/*.md` docs); Wave C wires the loose ends together.

## Read first
- `.planning/features/00-MASTER-PLAN.md` (naming, contracts, ownership — authoritative) and the all plan docs in `.planning/features/` relevant to the files you touch
- `.planning/features/CONTRACTS.md` (exact exported APIs from Wave A — use these, don't reinvent them)
- `AGENTS.md`: this is **Next.js 16** — APIs differ from your training data. Check `node_modules/next/dist/docs/` whenever unsure (params are Promises, `after` from `next/server`, route handlers, metadata, caching).
- `CLAUDE.md` (stack notes) and `src/db/schema.ts` (already migrated — every table you need exists).

## Rules
1. **Plan doc first**: before implementing each feature, write `.planning/features/C1-<slug>.md` (goal, UX, data/actions, files, edge cases — concise, ~20-40 lines). For integration fixes, one doc `C?-integration-fixes.md` listing each fix is enough.
2. **Strict file ownership** (shared working tree!): edit only the files listed under "You own" plus new files you create inside your own directories. Every slot/stub file assigned to you is yours to overwrite. Never edit another agent's files, even to fix a type error — note it under "Integration requests" in your final report instead. Everything not listed under "You own" is off-limits (especially `src/db/schema.ts` — the Wave C migration `0005` is already applied).
3. Don't run drizzle-kit, don't write to the database, don't install packages (ask via Integration requests if you truly need one — prefer the platform), no git, no `next build` / `next dev`, no new tests. If you need a schema change, STOP that part and report it.
4. **Verify** before finishing: `npx tsc --noEmit --incremental false 2>&1 | grep -E 'issues/|board/|views/table|selection|bulk|context-menu'` must show zero errors in your files (other agents are mid-flight; ignore errors in their files). `npx eslint <your files>` clean.
5. Style: match the codebase — sparse comments that explain *why*, shadcn components in `src/components/ui` (radix-nova; Popover, Command, DropdownMenu, ContextMenu, Dialog, AlertDialog, Sheet, Tooltip, HoverCard, Tabs, Switch, Select, Calendar, Checkbox, RadioGroup, ToggleGroup, Table, ScrollArea, Progress, Collapsible), domain glyphs in `src/components/ui-icons` (StatusIcon/StateIcon, PriorityIcon, LabelChip, Avatar, EmptyState, Skeleton), the reusable pickers in `src/components/issue-pickers/*`, lucide-react, Tailwind v4 tokens, sonner toasts, hotkey registry `src/lib/hotkeys.ts`, palette registry `src/lib/palette-commands.ts`. Linear look & feel: dense, quiet, keyboard-first, good empty states and loading states.
6. **Security**: every server action / route handler authorizes via `authorizeProjectAction` (or the project-access helpers) before reading or writing project data, and validates that every referenced id belongs to that project. Public endpoints (webhooks, API, intake) authenticate with their own secret/token and never leak other projects' data.
7. Mutations that change issues go through the existing actions in `src/app/actions/tickets.ts` (or your own actions that call `emitIssueEvent`) so activity history, notifications, Slack and webhooks all see them.
8. Keep the free-tier constraints: no paid services, no cron beyond lazy/throttled work, no websockets.

## Final report (your last message)
Features done (with plan doc paths); files created/modified; integration requests (exact file + exact change needed) for files you don't own; env vars added; anything unfinished or deviations.

## Your features
Bulk select and multi-edit (checklist item) — plus a right-click issue context menu (Linear parity; reuses the same actions).

## You own
- `src/components/issues/slots/bulk-bar.tsx` (stub → implement), `src/components/issues/selection.tsx` (new: selection store/provider + hook), `src/components/issues/issue-context-menu.tsx` (new)
- `src/components/issues/issue-list.tsx`, `issue-row.tsx`, `issues-view.tsx` (only to mount the selection provider / wire the bulk bar), `src/components/board/**`, `src/components/views/table-view.tsx`
- NOT yours: `use-issue-mutations.ts` (C2 edits it), filters/display files, slots other than bulk-bar.

## Build
- **Selection model**: per `IssuesView` selection set (ids) with anchor for ranges. List rows: hover checkbox left of the status icon (always visible once anything is selected); click toggles; Shift+click selects a range in visual order; ⌘/Ctrl+click toggles; `x` toggles the focused row (register via the hotkey registry, scope "Issues", with a `when` guard on `[data-issue-row]`/`[data-board-card]` focus); ⌘A selects all visible issues when focus is in the list/board/table (don't hijack ⌘A in inputs); Esc clears the selection (only when something is selected and no dialog/menu is open). Board cards: ⌘/Shift-click toggles, selected cards get a ring. Table: checkbox column + header "select all". Selected rows get a subtle highlight and `aria-selected`. Selection is pruned when issues disappear (filter change, deletion).
- **Bulk bar** (`BulkBar({ issues, mutations })`): floating bar at the bottom centre when ≥1 selected — "N selected", then actions using the shared pickers (`src/components/issue-pickers/*` popovers): Status, Priority, Assignee, Labels (add/remove semantics: show tri-state checks; since `bulkUpdate` with `labelIds` *replaces* each issue's set, compute per-issue sets and call `mutations.update` per issue, or group issues by resulting set and call `bulkUpdate` per group), Estimate (if the scale isn't none), Due date, Cycle (if cycles exist — use `useProjectData().cycles`), Epic, Archive, Move to trash (confirm), Clear (×). All other bulk actions go through `mutations.bulkUpdate` (one server call; B10 already pushes one undo entry for bulk edits and shows an Undo toast for 2+ issues). Keyboard: while the bar is shown, the bar is reachable via Tab; `⌘⌫` trash-with-confirm applies to the selection.
- **Context menu**: right-click on a list row, board card or table row opens a `ContextMenu` (shadcn `context-menu` installed) for that issue — or for the whole selection when the clicked issue is selected — with submenus Status / Priority / Assignee / Labels / Due date (quick options) and items Copy ID, Copy link, Open in new tab, Archive, Move to trash. Reuse `src/components/productivity/issue-actions.ts` (B10) for copy/archive/trash helpers if its exports fit; don't edit it.
- Guests (`useProjectPermission('write')` false): selection allowed for copy, but edit actions hidden/disabled.
- Keep all existing behaviour: j/k/Enter/s in the list, Space peek (B6), board Space pick-up + drag and drop (B5), table keyboard nav, manual-sort dragging.
