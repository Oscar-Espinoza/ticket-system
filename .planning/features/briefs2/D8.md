You are agent **D8 views & insights** in round 2 of a multi-agent build of Linear-parity features for a Next.js 16 + Drizzle + Neon ticket system at /home/oscar/Projects/ticket-system (branch `main`). About 12 other agents work IN PARALLEL in the same working tree, each owning different files. Round 1 built the core Linear feature set; Wave 0 of round 2 prepared schema, deps, auth plugins, cron, nav, slots and stubs for you.

## Read first
- `.planning/features/10-MASTER-PLAN-2.md` (round-2 plan, what Wave 0 already did — authoritative)
- `.planning/features/CONTRACTS.md` (exact exported APIs from round 1 — reuse them) and `.planning/features/00-MASTER-PLAN.md`
- the round-1 plan docs (`.planning/features/A*/B*/C*-*.md`) for any area you touch
- `AGENTS.md`: **Next.js 16** — APIs differ from your training data; check `node_modules/next/dist/docs/` when unsure. Library docs: read the installed package's types/README in `node_modules` rather than relying on memory (Tiptap 3, Better Auth 1.6.33 plugins, graphql-yoga 5, web-push, yjs).
- `CLAUDE.md` and `src/db/schema.ts` (already migrated — every table/column you need exists; read it).

## Rules
1. **Plan doc first**: for each checklist feature you own, write `.planning/features/D8-<slug>.md` (goal, UX, data/actions, files, edge cases; ~20–40 lines) BEFORE implementing it.
2. **Strict file ownership** (shared working tree): edit only files under "You own" plus new files you create inside your own new directories/routes. Stub/slot files assigned to you are yours to overwrite. Never edit anyone else's file, even to fix a type error — write it under "Integration requests" in your final report (exact file + exact change).
3. No schema changes, no drizzle-kit, no DB writes (read-only queries are fine), no package installs (report if one is truly needed), no git, no `next build` / `next dev`, no new tests.
4. **Verify** before finishing: `npx tsc --noEmit --incremental false 2>&1 | grep -E 'components/views|components/issues/(views|issue-row|issue-list|display-options|issue-filters)|components/board|issue-filtering|lib/search|dashboard/search|search-controls|dashboards|components/dashboards|lib/dashboards|insights|pulse|property-start-date'` shows zero errors in your files (others are mid-flight; ignore their errors), and `npx eslint <your files>` is clean.
5. Match the codebase: sparse comments that explain *why*; shadcn components in `src/components/ui`; domain glyphs in `src/components/ui-icons`; shared pickers in `src/components/issue-pickers`; lucide-react; Tailwind v4 tokens; sonner toasts; hotkey registry `src/lib/hotkeys.ts` (supports `shift`); palette registry `src/lib/palette-commands.ts`. Linear look and feel: dense, quiet, keyboard-first, good empty/loading states.
6. **Security**: every server action/route authorizes (`authorizeProjectAction`, project/workspace access helpers, API auth) before touching data and validates every referenced id. Public endpoints (webhooks, Slack, OAuth, SCIM, cron) verify their own signatures/tokens with timing-safe compares and never leak other projects' data.
7. Issue writes go through `src/lib/issue-service.ts` (actor-based) or the session actions in `src/app/actions/tickets.ts`, so activity, notifications, Slack and webhooks see them. Custom events include `data.summary`, `key`, `title`.
8. $0 only: free tiers, no paid services. Every external integration must degrade gracefully when its env vars are unset (explain in the UI what to configure).

## Final report (last message)
Features done (plan doc paths); files created/modified; integration requests (exact file + change); env vars used; anything unfinished or deviations.

## Your features
Dashboards · Pulse / project update digests · Issue timeline view · Search query syntax

## You own
- `src/components/views/**`, `src/components/issues/{views,issue-row,issue-list,display-options,issue-filters}.tsx`, `src/components/board/board-card.tsx` (only to render new chips), `src/lib/issue-filtering.ts`, `src/lib/search.ts`, `src/app/dashboard/search/**`, `src/components/navigation/search-controls.tsx`
- `src/lib/insights.ts`, `src/components/insights/**`, `src/app/dashboard/dashboards/**` (stubs), new `src/lib/dashboards.ts`, `src/app/actions/dashboards.ts`, `src/components/dashboards/**`
- `src/lib/cron/jobs/pulse.ts`, new `src/lib/pulse.ts`, `src/components/issue-detail/slots/property-start-date.tsx`
- NOT yours: `issues-view.tsx` shell props (don't change its props), `use-issue-mutations.ts` (D3), selection/bulk files (C1).

## Build
- **Dashboards** (`dashboard`): list at `/dashboard/dashboards` (mine + shared), detail page with a responsive grid of widgets (add/remove/reorder/resize via simple size presets), widget types: number (count of issues matching filters), bar/column chart (issues grouped by state/priority/assignee/label/customer), line (created vs completed over time), issue list (filters), cycle progress, epic progress, time-in-status (from `stateChangedAt`/activity). Widgets use a project scope + `IssueFilters` JSON (reuse `normalizeIssueFilters`/`filterIssues`) and the insights SQL (extend `src/lib/insights.ts`). Sharing (project members) + favorites (`FavoriteButton targetType` — 'dashboard' isn't a favorite target; skip or note).
- **Pulse**: weekly digest of project updates — the daily `pulse` job, on Mondays (or when 7 days passed since the user's last pulse), creates one `notification` (type `pulse`, data.summary + list of epic updates/health changes, issues completed/created counts per project) per member of each project with activity, and calls `deliverToChannels`. Add a "Pulse" page section (e.g. `/dashboard/dashboards?tab=pulse` or on the dashboards list) showing the latest updates feed across your projects.
- **Issue timeline view**: new layout `timeline` in the views registry — Gantt rows per issue (grouped by current grouping) from `startDate` (or `createdAt`) to `dueDate`, drag/resize to set `startDate`/`dueDate` via `mutations.update`, today line, zoom week/month; `PropertyStartDate` slot = start-date picker (reuse `DueDatePicker` with a different label).
- **Search query syntax**: parse `is:open|closed|archived`, `status:"In Progress"`, `type:started`, `assignee:me|name|none`, `creator:`, `priority:urgent|high|…`, `label:bug` (repeatable, `-label:` to exclude), `project:KEY`, `cycle:current`, `epic:"name"`, `due:overdue|today|week`, `created:>7d`, `sla:breached`, plus free text (full-text) — in `/dashboard/search` (server) and in the issues quick-filter box (client, maps to `IssueFilters` + text). Show a syntax help popover. Keep existing URL params working.
- Add an `sla` display property rendering `SlaChip` (from `src/components/sla/sla-chip.tsx`, D5) in rows/cards/table, and `startDate` column in the table view.
