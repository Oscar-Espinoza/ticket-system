You are agent **C3 data fixes** in a multi-agent build of Linear-parity features for a Next.js 16 + Drizzle + Neon ticket system at /home/oscar/Projects/ticket-system (branch feat/linear-parity). This is **Wave C** (integration). Two other Wave C agents work IN PARALLEL in the same working tree, each owning different files. Waves A and B already built every feature (see all `.planning/features/*.md` docs); Wave C wires the loose ends together.

## Read first
- `.planning/features/00-MASTER-PLAN.md` (naming, contracts, ownership — authoritative) and the all plan docs in `.planning/features/` relevant to the files you touch
- `.planning/features/CONTRACTS.md` (exact exported APIs from Wave A — use these, don't reinvent them)
- `AGENTS.md`: this is **Next.js 16** — APIs differ from your training data. Check `node_modules/next/dist/docs/` whenever unsure (params are Promises, `after` from `next/server`, route handlers, metadata, caching).
- `CLAUDE.md` (stack notes) and `src/db/schema.ts` (already migrated — every table you need exists).

## Rules
1. **Plan doc first**: before implementing each feature, write `.planning/features/C3-<slug>.md` (goal, UX, data/actions, files, edge cases — concise, ~20-40 lines). For integration fixes, one doc `C?-integration-fixes.md` listing each fix is enough.
2. **Strict file ownership** (shared working tree!): edit only the files listed under "You own" plus new files you create inside your own directories. Every slot/stub file assigned to you is yours to overwrite. Never edit another agent's files, even to fix a type error — note it under "Integration requests" in your final report instead. Everything not listed under "You own" is off-limits (especially `src/db/schema.ts` — the Wave C migration `0005` is already applied).
3. Don't run drizzle-kit, don't write to the database, don't install packages (ask via Integration requests if you truly need one — prefer the platform), no git, no `next build` / `next dev`, no new tests. If you need a schema change, STOP that part and report it.
4. **Verify** before finishing: `npx tsc --noEmit --incremental false 2>&1 | grep -E 'integrations/|actions/integrations|settings/integrations|planning-settings|cycles|automation|issue-service|actions/import|notifications/dispatch|github/sync|workspace|invite/workspace'` must show zero errors in your files (other agents are mid-flight; ignore errors in their files). `npx eslint <your files>` clean.
5. Style: match the codebase — sparse comments that explain *why*, shadcn components in `src/components/ui` (radix-nova; Popover, Command, DropdownMenu, ContextMenu, Dialog, AlertDialog, Sheet, Tooltip, HoverCard, Tabs, Switch, Select, Calendar, Checkbox, RadioGroup, ToggleGroup, Table, ScrollArea, Progress, Collapsible), domain glyphs in `src/components/ui-icons` (StatusIcon/StateIcon, PriorityIcon, LabelChip, Avatar, EmptyState, Skeleton), the reusable pickers in `src/components/issue-pickers/*`, lucide-react, Tailwind v4 tokens, sonner toasts, hotkey registry `src/lib/hotkeys.ts`, palette registry `src/lib/palette-commands.ts`. Linear look & feel: dense, quiet, keyboard-first, good empty states and loading states.
6. **Security**: every server action / route handler authorizes via `authorizeProjectAction` (or the project-access helpers) before reading or writing project data, and validates that every referenced id belongs to that project. Public endpoints (webhooks, API, intake) authenticate with their own secret/token and never leak other projects' data.
7. Mutations that change issues go through the existing actions in `src/app/actions/tickets.ts` (or your own actions that call `emitIssueEvent`) so activity history, notifications, Slack and webhooks all see them.
8. Keep the free-tier constraints: no paid services, no cron beyond lazy/throttled work, no websockets.

## Final report (your last message)
Features done (with plan doc paths); files created/modified; integration requests (exact file + exact change needed) for files you don't own; env vars added; anything unfinished or deviations.

## Your task
Wire the new schema (migration `0005`, already applied) and fix the queued data-layer issues. Write `.planning/features/C3-integration-fixes.md` first (one line per fix), then implement.

## You own (only for the listed fixes)
`src/lib/integrations/{event-types,slack,outgoing-webhooks}.ts`, `src/app/actions/integrations.ts`, `src/app/dashboard/projects/[id]/settings/integrations/**`, `src/components/integrations/slack-settings.tsx`, `src/app/actions/planning-settings.ts`, `src/components/cycles/**`, `src/lib/cycles.ts`, `src/lib/automation.ts`, `src/app/dashboard/projects/[id]/settings/planning/**`, `src/lib/issue-service.ts` (additive option only), `src/app/actions/import.ts`, `src/lib/notifications/dispatch.ts`, `src/lib/github/sync.ts`, `src/lib/workspace-access.ts`, `src/app/actions/workspaces.ts`, `src/components/workspaces/**`, `src/app/invite/workspace/**`, `src/app/dashboard/workspaces/[slug]/page.tsx`.

## New schema (in `src/db/schema.ts`, read it)
- `project.slackEvents` jsonb `string[] | null` (null = all kinds)
- `project.cycleStartWeekday` integer not null default 1 (0 = Sunday … 6 = Saturday, UTC)
- `project.cycleAutoRollover` boolean not null default true
- table `workspace_invitation` (`workspaceInvitations`): id, workspaceId, email, role (admin|member), token unique, invitedById, acceptedAt, expiresAt, createdAt

## Fixes
1. **Slack events column**: stop storing event toggles in the URL fragment. `parseSlackSetting` / `formatSlackSetting` in `event-types.ts` become column-based (e.g. `parseSlackSetting(url, events)`); update every caller (`actions/integrations.ts`, the integrations settings page, `slack.ts`). Backwards compatible read: if an existing `slackWebhookUrl` still contains `#events=…`, strip the fragment and migrate the kinds into `slackEvents` on the next save (reads should treat a fragment as the event list when `slackEvents` is null).
2. **Cycle start weekday + auto-rollover** (B7): replace the "derive weekday from the latest cycle" workaround with `project.cycleStartWeekday`, and the always-on rollover with `project.cycleAutoRollover`, in the planning settings action + form (a weekday select and a rollover switch), in `src/lib/cycles.ts` scheduling and in `src/lib/automation.ts` (`completeEndedCycles`). Keep the "changing weekday/duration reschedules not-yet-started cycles" behaviour. Update `.planning/features/B7-cycles.md` / `B7-automations.md` notes.
3. **Bulk-import noise** (B12): add an optional 4th arg to `createIssue` in `issue-service.ts` — `opts?: { source?: 'import' }` — that adds `data.bulk: true` to the `issue.created` event (don't change other behaviour or signatures). Pass it from `src/app/actions/import.ts`. Then `postToSlack`, `deliverWebhooks` and `dispatchNotifications` skip events with `data.bulk === true` (webhooks: skip too — document that imports don't fire webhooks). One summary is fine but optional.
4. **Project-level events** (`ticketId: null`, e.g. `member.role_changed`, `workflow_state.*`, `epic.update_posted`, `project.ownership_transferred`): make sure notifications dispatch, Slack and outgoing webhooks ignore or safely handle them (no crashes, no queries with a null ticket id). Read each dispatcher and fix as needed.
5. **PR-merge double notification**: when a merged PR also completes the issue, subscribers currently get a `github` "Merged PR" notification and a `completed` one. Collapse to one: e.g. in `src/lib/github/sync.ts` mark the state-change event with `data.viaPullRequest` (number/url) and skip the separate `github.pr_merged` notification in dispatch when the same sync also completed the issue — or the reverse; pick the cleanest and keep the activity rows intact (only notifications collapse).
6. **Workspace invitations table** (B9): replace the stateless HMAC tokens with `workspace_invitation` rows: create (random token, 7-day expiry, role), list pending invitations on the workspace page (members section) with resend / copy link / revoke (admin+), accept at `/invite/workspace/[token]` (signed-in email must match, single use via conditional UPDATE on `acceptedAt IS NULL AND expiresAt > now()`, idempotent membership insert). Remove `signWorkspaceInvite`/`verifyWorkspaceInvite` if nothing else uses them. Keep the "existing co-member is added directly" path and the non-enumerating response.
