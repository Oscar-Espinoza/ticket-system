You are agent **B11 integrations** in a multi-agent build of Linear-parity features for a Next.js 16 + Drizzle + Neon ticket system at /home/oscar/Projects/ticket-system (branch feat/linear-parity). About 11 other Wave B agents work IN PARALLEL in the same working tree, each owning different files. Wave A already built the foundation (data layer, workflow states, pickers, slots, navigation, settings scaffolds, stub pages).

## Read first
- `.planning/features/00-MASTER-PLAN.md` (naming, contracts, ownership — authoritative) and the Wave A plan docs in `.planning/features/A1-*.md`, `A2-*.md`
- `.planning/features/CONTRACTS.md` (exact exported APIs from Wave A — use these, don't reinvent them)
- `AGENTS.md`: this is **Next.js 16** — APIs differ from your training data. Check `node_modules/next/dist/docs/` whenever unsure (params are Promises, `after` from `next/server`, route handlers, metadata, caching).
- `CLAUDE.md` (stack notes) and `src/db/schema.ts` (already migrated — every table you need exists).

## Rules
1. **Plan doc first**: before implementing each feature, write `.planning/features/B11-<slug>.md` (goal, UX, data/actions, files, edge cases — concise, ~20-40 lines). One doc per checklist feature.
2. **Strict file ownership** (shared working tree!): edit only the files listed under "You own" plus new files you create inside your own directories. Every slot/stub file assigned to you is yours to overwrite. Never edit another agent's files, even to fix a type error — note it under "Integration requests" in your final report instead. Frozen hosts (don't edit): `src/components/issue-detail/issue-detail.tsx`, `src/components/issue-detail/issue-detail-pane.tsx`, `src/app/dashboard/projects/[id]/layout.tsx`, `src/components/app-shell/shell-frame.tsx`, `src/lib/issue-model.ts`, `src/lib/tickets.ts`, `src/lib/events.ts`, `src/lib/action-auth.ts`, `src/lib/project-data.ts`, `src/app/actions/tickets.ts`, `src/db/schema.ts` — unless listed under "You own".
3. Don't run drizzle-kit, don't write to the database, don't install packages (ask via Integration requests if you truly need one — prefer the platform), no git, no `next build` / `next dev`, no new tests. If you need a schema change, STOP that part and report it.
4. **Verify** before finishing: `npx tsc --noEmit --incremental false 2>&1 | grep -E 'integrations|api/v1|api-keys|api-auth|settings/intake|request/|actions/intake'` must show zero errors in your files (other agents are mid-flight; ignore errors in their files). `npx eslint <your files>` clean.
5. Style: match the codebase — sparse comments that explain *why*, shadcn components in `src/components/ui` (radix-nova; Popover, Command, DropdownMenu, ContextMenu, Dialog, AlertDialog, Sheet, Tooltip, HoverCard, Tabs, Switch, Select, Calendar, Checkbox, RadioGroup, ToggleGroup, Table, ScrollArea, Progress, Collapsible), domain glyphs in `src/components/ui-icons` (StatusIcon/StateIcon, PriorityIcon, LabelChip, Avatar, EmptyState, Skeleton), the reusable pickers in `src/components/issue-pickers/*`, lucide-react, Tailwind v4 tokens, sonner toasts, hotkey registry `src/lib/hotkeys.ts`, palette registry `src/lib/palette-commands.ts`. Linear look & feel: dense, quiet, keyboard-first, good empty states and loading states.
6. **Security**: every server action / route handler authorizes via `authorizeProjectAction` (or the project-access helpers) before reading or writing project data, and validates that every referenced id belongs to that project. Public endpoints (webhooks, API, intake) authenticate with their own secret/token and never leak other projects' data.
7. Mutations that change issues go through the existing actions in `src/app/actions/tickets.ts` (or your own actions that call `emitIssueEvent`) so activity history, notifications, Slack and webhooks all see them.
8. Keep the free-tier constraints: no paid services, no cron beyond lazy/throttled work, no websockets.

## Final report (your last message)
Features done (with plan doc paths); files created/modified; integration requests (exact file + exact change needed) for files you don't own; env vars added; anything unfinished or deviations.

## Your features
Slack integration · Public API · Outgoing webhooks · Customer requests / intake forms

## You own
- `src/lib/integrations/slack.ts`, `src/lib/integrations/outgoing-webhooks.ts` (stubs — implement), `src/lib/api-auth.ts` (new)
- `src/app/dashboard/projects/[id]/settings/integrations/**`, `.../settings/intake/**`, `src/app/dashboard/settings/api-keys/**` (stubs — overwrite)
- `src/app/api/v1/**` (new), `src/app/request/[token]/**` (new public page)
- `src/app/actions/integrations.ts`, `src/app/actions/api-keys.ts`, `src/app/actions/intake.ts` (new), `src/components/integrations/**` (new)

## Build
- **Slack** (per project, admin): incoming-webhook URL (validate `https://hooks.slack.com/`), toggles for which events post (issue created, completed, canceled, commented), "Send test message". `postToSlack(events)` formats Slack Block Kit messages with issue key/title linking to the permalink, actor name, state; 3 s timeout; never throws.
- **Outgoing webhooks** (per project, admin): CRUD (URL https only in production, events multi-select from the event types, enabled toggle), secret generated + revealable, "Send test", last delivery status/time. `deliverWebhooks(events)`: JSON payload `{ id, type, createdAt, projectId, issue: { id, key, title, state, priority, assignee, url }, data }`, header `X-Webhook-Signature: sha256=<hmac hex>` + `X-Webhook-Event`, 5 s timeout, record `lastStatus`/`lastDeliveredAt`; never throws. SSRF guard: reject private/loopback hosts unless `NODE_ENV !== 'production'`.
- **API keys** `/dashboard/settings/api-keys`: create (name) → show the key once (`lc_<random>`; store sha256 + prefix), list (name, prefix, created, last used), revoke. `src/lib/api-auth.ts`: `authenticateApiRequest(req)` from `Authorization: Bearer`, updates `lastUsedAt` (throttled).
- **Public REST API** `/api/v1` (JSON, key = acts as that user, membership-checked on every call, consistent `{ error }` bodies with proper status codes, simple per-key rate limit is optional): `GET /me`, `GET /projects`, `GET /projects/:id/states|labels|members`, `GET /projects/:id/issues` (filters: state, assignee, label, updated_since; cursor pagination by number), `POST /projects/:id/issues`, `GET|PATCH|DELETE /issues/:key` (key like `APP-12`), `GET|POST /issues/:key/comments` (insert comment rows directly + emit `comment.created` if B1's actions aren't reusable without a session). Use the issue service layer with `actor { userId }`. Add `docs/API.md` documenting endpoints with curl examples.
- **Intake form** (customer requests): settings page toggles the form (generates/rotates `project.intakeToken`, shows the public URL and an embeddable snippet) and lists received requests (`customer_request` rows with links to their issues). Public page `/request/[token]` (no auth, outside `/dashboard`): name, email, title, description, honeypot field + minimum-time check; creates the issue via the issue service with `actor { userId: null }` in the project's triage state when triage is enabled (else the default new-issue state), stores the `customer_request` row, shows a thank-you state. Never reveal project internals beyond the project name.
