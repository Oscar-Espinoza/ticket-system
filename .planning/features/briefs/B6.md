You are agent **B6 navigation** in a multi-agent build of Linear-parity features for a Next.js 16 + Drizzle + Neon ticket system at /home/oscar/Projects/ticket-system (branch feat/linear-parity). About 11 other Wave B agents work IN PARALLEL in the same working tree, each owning different files. Wave A already built the foundation (data layer, workflow states, pickers, slots, navigation, settings scaffolds, stub pages).

## Read first
- `.planning/features/00-MASTER-PLAN.md` (naming, contracts, ownership — authoritative) and the Wave A plan docs in `.planning/features/A1-*.md`, `A2-*.md`
- `.planning/features/CONTRACTS.md` (exact exported APIs from Wave A — use these, don't reinvent them)
- `AGENTS.md`: this is **Next.js 16** — APIs differ from your training data. Check `node_modules/next/dist/docs/` whenever unsure (params are Promises, `after` from `next/server`, route handlers, metadata, caching).
- `CLAUDE.md` (stack notes) and `src/db/schema.ts` (already migrated — every table you need exists).

## Rules
1. **Plan doc first**: before implementing each feature, write `.planning/features/B6-<slug>.md` (goal, UX, data/actions, files, edge cases — concise, ~20-40 lines). One doc per checklist feature.
2. **Strict file ownership** (shared working tree!): edit only the files listed under "You own" plus new files you create inside your own directories. Every slot/stub file assigned to you is yours to overwrite. Never edit another agent's files, even to fix a type error — note it under "Integration requests" in your final report instead. Frozen hosts (don't edit): `src/components/issue-detail/issue-detail.tsx`, `src/components/issue-detail/issue-detail-pane.tsx`, `src/app/dashboard/projects/[id]/layout.tsx`, `src/components/app-shell/shell-frame.tsx`, `src/lib/issue-model.ts`, `src/lib/tickets.ts`, `src/lib/events.ts`, `src/lib/action-auth.ts`, `src/lib/project-data.ts`, `src/app/actions/tickets.ts`, `src/db/schema.ts` — unless listed under "You own".
3. Don't run drizzle-kit, don't write to the database, don't install packages (ask via Integration requests if you truly need one — prefer the platform), no git, no `next build` / `next dev`, no new tests. If you need a schema change, STOP that part and report it.
4. **Verify** before finishing: `npx tsc --noEmit --incremental false 2>&1 | grep -E 'my-issues|dashboard/search|dashboard/views|/views/|issues/\[key\]|/archive|/trash|sidebar-favorites|header-favorite|toolbar-extra|list-overlay|components/navigation|actions/views|actions/favorites|lib/search|lib/views|lib/favorites'` must show zero errors in your files (other agents are mid-flight; ignore errors in their files). `npx eslint <your files>` clean.
5. Style: match the codebase — sparse comments that explain *why*, shadcn components in `src/components/ui` (radix-nova; Popover, Command, DropdownMenu, ContextMenu, Dialog, AlertDialog, Sheet, Tooltip, HoverCard, Tabs, Switch, Select, Calendar, Checkbox, RadioGroup, ToggleGroup, Table, ScrollArea, Progress, Collapsible), domain glyphs in `src/components/ui-icons` (StatusIcon/StateIcon, PriorityIcon, LabelChip, Avatar, EmptyState, Skeleton), the reusable pickers in `src/components/issue-pickers/*`, lucide-react, Tailwind v4 tokens, sonner toasts, hotkey registry `src/lib/hotkeys.ts`, palette registry `src/lib/palette-commands.ts`. Linear look & feel: dense, quiet, keyboard-first, good empty states and loading states.
6. **Security**: every server action / route handler authorizes via `authorizeProjectAction` (or the project-access helpers) before reading or writing project data, and validates that every referenced id belongs to that project. Public endpoints (webhooks, API, intake) authenticate with their own secret/token and never leak other projects' data.
7. Mutations that change issues go through the existing actions in `src/app/actions/tickets.ts` (or your own actions that call `emitIssueEvent`) so activity history, notifications, Slack and webhooks all see them.
8. Keep the free-tier constraints: no paid services, no cron beyond lazy/throttled work, no websockets.

## Final report (your last message)
Features done (with plan doc paths); files created/modified; integration requests (exact file + exact change needed) for files you don't own; env vars added; anything unfinished or deviations.

## Your features
My Issues · Custom saved views · Favorites · Full-text issue search · Archive / archived issues · Trash / restore deleted issues · Peek preview · Issue permalink page

## You own
- Pages: `src/app/dashboard/my-issues/**`, `src/app/dashboard/search/**`, `src/app/dashboard/views/**`, `src/app/dashboard/projects/[id]/views/**`, `src/app/dashboard/projects/[id]/issues/[key]/**`, `src/app/dashboard/projects/[id]/archive/**`, `src/app/dashboard/projects/[id]/trash/**` (stubs from A2 — overwrite)
- `src/components/navigation/**` (includes the pre-created `favorite-button.tsx`), `src/app/actions/favorites.ts` (pre-created), `src/app/actions/views.ts`, `src/lib/search.ts`, `src/lib/views.ts`, `src/lib/favorites.ts` (new)
- Slots: `src/components/app-shell/slots/sidebar-favorites.tsx`, `src/components/issue-detail/slots/header-favorite.tsx`, `src/components/issues/slots/toolbar-extra.tsx`, `src/components/issues/slots/list-overlay.tsx`

## Build
- **Permalink** `/dashboard/projects/[id]/issues/[key]` (key like `APP-12`, case-insensitive): full-page `IssueDetail variant="page"` with working mutations (load the project's issues with `getProjectIssues` so sub-issue sections work), archived/deleted issues render with a banner + Unarchive/Restore; `notFound()` otherwise; metadata title `KEY title`. Render the `IssueShortcuts` slot too if it exists. Link every issue key/title reference you render to this page.
- **Peek preview** (`list-overlay.tsx`): with an issue row focused in list view, **Space** opens a read-only preview dialog (key, title, state/priority/assignee/labels, description via `Markdown` if available) — Space/Esc closes, Enter opens the full issue. Register in the hotkey registry (scope "Issues") with a `when` guard on `[data-issue-row]` focus so the board's Space pick-up is unaffected.
- **My Issues** `/dashboard/my-issues`: tabs Assigned / Created / Subscribed / Recent activity across every project the user is a member of (authorize via membership join), excluding archived/deleted; grouped by state type (started → unstarted → backlog → triage → completed/canceled collapsed); rows link to permalinks; quick filters (hide completed). Use `queryIssues` from `src/lib/tickets.ts` with your own `where`.
- **Search** `/dashboard/search?q=`: Postgres full-text across member projects — must use exactly the indexed expression `to_tsvector('english', coalesce(title,'') || ' ' || coalesce(description,''))` with `websearch_to_tsquery('english', q)`, ranked by `ts_rank`, plus comment bodies (`to_tsvector('english', body)`), plus exact key matches (`APP-12`) first; `ts_headline` snippets (escape HTML — render as text with <mark> segments you build safely); filters by project and "include archived". Register a palette command "Search issues…" and make the search page input autofocus.
- **Saved views**: `ToolbarExtra` shows "Save view" (dialog: name, description, shared with project toggle) capturing current `IssueFilters` + `DisplayOptions` (from B5's `useIssueFilters()` / `useDisplayOptions()`); when `savedView` is set, show "Update view" / "Save as new". Pages: `/dashboard/views` (your views + shared views across your projects; open, rename, delete, favorite), `/dashboard/projects/[id]/views` (project views list), `/dashboard/projects/[id]/views/[viewId]` (renders `IssuesView` with `initialFilters`, `initialDisplay`, `savedView`), `/dashboard/views/[viewId]` redirects to the project view page. Owner edits; shared views visible to project members.
- **Favorites**: star toggles (`FavoriteButton targetType targetId`) in the issue header slot and on view rows; sidebar section `SidebarFavorites` listing favorites (issues, projects, views, epics, cycles, initiatives — resolve names/links server-side, skip ones the user can no longer access) with remove on hover. Reorder optional.
- **Archive** `/projects/[id]/archive`: archived issues (list, search) with Unarchive. **Trash** `/projects/[id]/trash`: deleted issues with Restore and "Delete permanently" (admins; confirm) — note that items are purged after 30 days (B7's automation does the purge).

## Notes from Wave A
- `IssueRow`, `BoardCard`, `IssueDetail` require `useProjectData()`, so cross-project lists (My Issues, search) need your own lightweight row component (`src/components/navigation/`).
- You also own `src/lib/favorite-targets.ts` (pre-created).
- `useIssueMutations(serverIssues, { include })` — archive/trash pages pass a predicate (e.g. `i => !!i.archivedAt && !i.deletedAt`).
