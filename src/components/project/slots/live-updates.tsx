'use client';

// Owner: B12 (data & realtime). Stub — mounted once by the project layout.
// Poll a cheap change-token endpoint and router.refresh() on change; presence.

export function LiveUpdates({ projectId }: { projectId: string }) {
  void projectId;
  return null;
}
