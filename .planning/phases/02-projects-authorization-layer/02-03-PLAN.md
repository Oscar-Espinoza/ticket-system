---
phase: 02-projects-authorization-layer
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - src/components/project-list.tsx
  - src/app/dashboard/page.tsx
autonomous: true
requirements: [PROJ-02]
must_haves:
  truths:
    - "The dashboard lists only projects the signed-in user owns or is a member of"
    - "Each project card shows the name, ticket-key badge, owner/member role badge, and open vs resolved ticket counts"
    - "Open count = tickets with status != 'done'; resolved count = tickets with status = 'done'; both are numbers (0/0 until Phase 5)"
    - "With no projects, the dashboard shows a 'No projects yet' empty state with a New project CTA"
    - "The New project Dialog is reachable from the dashboard"
  artifacts:
    - path: "src/components/project-list.tsx"
      provides: "Server Component project list + getProjectsForUser query"
      exports: ["ProjectList", "getProjectsForUser"]
      contains: "innerJoin"
    - path: "src/app/dashboard/page.tsx"
      provides: "dashboard shell wiring ProjectList + CreateProjectDialog into the children seam"
  key_links:
    - from: "src/app/dashboard/page.tsx"
      to: "src/components/project-list.tsx + src/components/create-project-dialog.tsx"
      via: "render ProjectList and CreateProjectDialog in the content section"
      pattern: "ProjectList"
    - from: "src/components/project-list.tsx"
      to: "/dashboard/projects/[id]"
      via: "Link wrapping each project Card"
      pattern: "/dashboard/projects/"
---

<objective>
Build the project-list vertical slice (PROJ-02): a Server Component that runs the
owned-or-member project query (INNER JOIN project_member, LEFT JOIN tickets with
conditional open/resolved counts), renders clickable project cards or a "No projects
yet" empty state, and is wired into the dashboard alongside the create-project Dialog
from Plan 02.

Purpose: After this slice a user lands on the dashboard and sees exactly the projects
they belong to (owner or member), each card linking to its detail page, with a CTA to
create more. The INNER JOIN on project_member is itself the authorization filter — no
cross-tenant rows can appear.
Output: `src/components/project-list.tsx`, and an additive edit to
`src/app/dashboard/page.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-projects-authorization-layer/02-RESEARCH.md
@.planning/phases/02-projects-authorization-layer/02-PATTERNS.md
@.planning/phases/02-projects-authorization-layer/02-UI-SPEC.md
@.planning/phases/02-projects-authorization-layer/02-CONTEXT.md

<interfaces>
<!-- Session pattern (server): auth.api.getSession({ headers: await headers() }); user.id -->
<!-- Query (RESEARCH Pattern 3 — owned-or-member with ticket counts): -->
db.select({
  id, name, ticketKey, createdAt, role: projectMembers.role,
  openCount: sql<number>`cast(count(case when ${tickets.status} != 'done' and ${tickets.id} is not null then 1 end) as int)`,
  resolvedCount: sql<number>`cast(count(case when ${tickets.status} = 'done' then 1 end) as int)`,
})
  .from(projectMembers)
  .innerJoin(projects, eq(projectMembers.projectId, projects.id))
  .leftJoin(tickets, eq(tickets.projectId, projects.id))
  .where(eq(projectMembers.userId, userId))
  .groupBy(projects.id, projects.name, projects.ticketKey, projects.createdAt, projectMembers.role)
  .orderBy(sql`${projects.createdAt} desc`)
// cast(... as int) is REQUIRED — bigint counts would otherwise type as string.

<!-- Components available: -->
import { CreateProjectDialog } from '@/components/create-project-dialog'; // Plan 02
import { Card, CardContent } from '@/components/ui/card';
import { Badge } from '@/components/ui/badge';
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement getProjectsForUser query + ProjectList Server Component</name>
  <files>src/components/project-list.tsx</files>
  <behavior>
    - getProjectsForUser(userId) returns rows for every project where the user has a project_member row (owner OR member) and none they do not belong to.
    - openCount and resolvedCount are JS numbers; a project with no tickets yields 0 and 0 (PROJ-02 zero-state test GREEN).
    - Rows are ordered newest-first by project createdAt.
  </behavior>
  <read_first>
    - src/app/dashboard/page.tsx (Server Component analog: session read, Badge usage, JSX structure; the {children} seam)
    - src/db/schema.ts (projects, projectMembers, tickets columns + status enum values)
    - src/lib/db.ts (the `db` neon-http export)
    - src/components/ui/card.tsx, src/components/ui/badge.tsx (primitives + variants)
    - .planning/phases/02-projects-authorization-layer/02-PATTERNS.md (§src/components/project-list.tsx — query, card-as-link, badge variant deltas, "render into children, no full-page wrapper")
    - .planning/phases/02-projects-authorization-layer/02-RESEARCH.md (§Pattern 3; §Code Examples project list query — cast as int note)
    - .planning/phases/02-projects-authorization-layer/02-UI-SPEC.md (§Dashboard project list layout, §Empty State, §Copywriting Contract)
    - .planning/phases/02-projects-authorization-layer/02-CONTEXT.md (D-19, D-20)
  </read_first>
  <action>
    Create `src/components/project-list.tsx`. Export `async function getProjectsForUser(
    userId: string)` implementing the RESEARCH Pattern 3 query exactly: import `sql, eq`
    from `drizzle-orm` and `projects, projectMembers, tickets` from `@/db/schema`; select
    id/name/ticketKey/createdAt/role plus `openCount` (`cast(count(case when
    ${tickets.status} != 'done' and ${tickets.id} is not null then 1 end) as int)`) and
    `resolvedCount` (`cast(count(case when ${tickets.status} = 'done' then 1 end) as
    int)`); `.from(projectMembers).innerJoin(projects, ...).leftJoin(tickets, ...)
    .where(eq(projectMembers.userId, userId))` then groupBy the five non-aggregate
    columns and `orderBy(sql\`${projects.createdAt} desc\`)`. The INNER JOIN on
    project_member is the authorization filter (covers owner + member; no OR needed).
    Export an `async function ProjectList()` Server Component that resolves the session
    via `auth.api.getSession({ headers: await headers() })`, calls getProjectsForUser(
    user.id), and renders: a section header row (`flex items-center justify-between`) with
    an h3 "Projects" (16px/600) and `<CreateProjectDialog />` on the right; then either
    the empty state (centered "No projects yet" + "Create your first project to get
    started." + a second `<CreateProjectDialog />` CTA) when zero rows, or a
    `flex flex-col gap-3` stack of cards. Each card: a Next.js `<Link href={
    \`/dashboard/projects/${p.id}\`}>` wrapping a `Card className="cursor-pointer
    hover:bg-accent/50 transition-colors"` → CardContent p-4 with a single row: left group
    = project name (14px/600) + ticket-key Badge (variant="secondary" font-mono) + role
    Badge (owner → variant="secondary" text "Owner"; member → variant="outline" text
    "Member"); right group = `{openCount} open · {resolvedCount} resolved` as
    text-sm text-muted-foreground. Render ONLY the section content (no min-h-screen / main
    wrapper) — it lives inside the dashboard shell's content area. Use exact copy from
    02-UI-SPEC §Copywriting Contract.
  </action>
  <verify>
    <automated>npx vitest run src/tests/projects.test.ts -t "PROJ-02"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/project-list.tsx` exports `getProjectsForUser` and `ProjectList`, and contains `innerJoin` + `leftJoin` + the two `cast(... as int)` conditional counts.
    - The two PROJ-02 tests pass (owner-and-member listing; 0 open / 0 resolved for ticketless project): `npx vitest run src/tests/projects.test.ts -t "PROJ-02"` exits 0.
    - Each card is a `<Link>` to `/dashboard/projects/{id}` and shows key badge + role badge + counts.
    - ProjectList renders section content only (no full-page wrapper).
  </acceptance_criteria>
  <done>getProjectsForUser returns owner+member projects with numeric open/resolved counts; ProjectList renders cards or the empty state; PROJ-02 tests GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Wire ProjectList + CreateProjectDialog into the dashboard</name>
  <files>src/app/dashboard/page.tsx</files>
  <read_first>
    - src/app/dashboard/page.tsx (the file being modified — existing imports, the {children} seam at lines ~75-77, the main content container)
    - src/components/project-list.tsx (ProjectList — created in Task 1; it already renders the CreateProjectDialog and section header)
    - .planning/phases/02-projects-authorization-layer/02-PATTERNS.md (§src/app/dashboard/page.tsx — additive modification, existing seam, do not rewrite shell)
    - .planning/phases/02-projects-authorization-layer/02-UI-SPEC.md (§Dashboard project list — section sits below greeting/GitHub badge with mt-8 separation)
    - .planning/phases/02-projects-authorization-layer/02-CONTEXT.md (D-19 children seam)
  </read_first>
  <action>
    Make an ADDITIVE edit to `src/app/dashboard/page.tsx` — do NOT rewrite the shell, the
    greeting, the GitHub status badge, or the logout/header. Import `ProjectList` from
    `@/components/project-list`. Render `<ProjectList />` inside the existing main content
    container, in/replacing the `{children}` seam region (the comment "Phase 2 seam"),
    below the greeting + GitHub badge block, with `mt-8` top separation per the UI-SPEC.
    Because `ProjectList` already renders its own section header with the
    `CreateProjectDialog` button, do NOT add a second dialog here. Keep the page a Server
    Component (it already reads the session) — ProjectList resolves its own session, so no
    new props are required. Confirm no other section of the file changes.
  </action>
  <verify>
    <automated>grep -q 'ProjectList' src/app/dashboard/page.tsx &amp;&amp; npx tsc --noEmit -p tsconfig.json &amp;&amp; npx next build --no-lint 2>/dev/null || npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/dashboard/page.tsx` imports and renders `<ProjectList />` in the content section.
    - The existing greeting, GitHub status badge, and header/logout are unchanged (additive edit only).
    - `npx tsc --noEmit` passes.
    - No duplicate CreateProjectDialog is rendered directly in page.tsx (it comes from ProjectList).
  </acceptance_criteria>
  <done>The dashboard renders the project list + New project CTA in the content seam without altering the Phase 1 shell; type-check passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated client → dashboard Server Component | The list must expose only the viewer's own memberships. |
| Server Component → neon-http `db` | The membership join is the row-level authorization filter. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Information Disclosure | project list query | mitigate | `INNER JOIN project_member ... WHERE projectMembers.userId = userId` restricts rows to the viewer's memberships only — no cross-tenant project can appear. Verified by the PROJ-02 owner+member test (and the absence of non-member rows). |
| T-02-08 | Spoofing | viewer identity for the list | mitigate | `userId` comes from `auth.api.getSession({ headers: await headers() })` server-side, never from client input. |
| T-02-09 | Tampering / correctness | bigint count typed as string | accept→mitigate | `cast(count(...) as int)` ensures numeric open/resolved counts; covered by the PROJ-02 zero-state assertion. |
</threat_model>

<verification>
- `npx vitest run src/tests/projects.test.ts -t "PROJ-02"` exits 0.
- `npx tsc --noEmit` passes.
- Manual (02-VALIDATION §Manual-Only): create a project, confirm a card appears with the owner badge + 0 open / 0 resolved.
</verification>

<success_criteria>
- Dashboard lists only owned-or-member projects, newest first.
- Each card shows name + key badge + role badge + open/resolved counts and links to the detail page.
- Empty state renders with a New project CTA; the dialog is reachable from the dashboard.
</success_criteria>

<output>
Create `.planning/phases/02-projects-authorization-layer/02-03-SUMMARY.md` when done.
</output>
