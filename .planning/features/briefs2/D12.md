You are agent **D12 security** in round 2 of a multi-agent build of Linear-parity features for a Next.js 16 + Drizzle + Neon ticket system at /home/oscar/Projects/ticket-system (branch `main`). About 12 other agents work IN PARALLEL in the same working tree, each owning different files. Round 1 built the core Linear feature set; Wave 0 of round 2 prepared schema, deps, auth plugins, cron, nav, slots and stubs for you.

## Read first
- `.planning/features/10-MASTER-PLAN-2.md` (round-2 plan, what Wave 0 already did — authoritative)
- `.planning/features/CONTRACTS.md` (exact exported APIs from round 1 — reuse them) and `.planning/features/00-MASTER-PLAN.md`
- the round-1 plan docs (`.planning/features/A*/B*/C*-*.md`) for any area you touch
- `AGENTS.md`: **Next.js 16** — APIs differ from your training data; check `node_modules/next/dist/docs/` when unsure. Library docs: read the installed package's types/README in `node_modules` rather than relying on memory (Tiptap 3, Better Auth 1.6.33 plugins, graphql-yoga 5, web-push, yjs).
- `CLAUDE.md` and `src/db/schema.ts` (already migrated — every table/column you need exists; read it).

## Rules
1. **Plan doc first**: for each checklist feature you own, write `.planning/features/D12-<slug>.md` (goal, UX, data/actions, files, edge cases; ~20–40 lines) BEFORE implementing it.
2. **Strict file ownership** (shared working tree): edit only files under "You own" plus new files you create inside your own new directories/routes. Stub/slot files assigned to you are yours to overwrite. Never edit anyone else's file, even to fix a type error — write it under "Integration requests" in your final report (exact file + exact change).
3. No schema changes, no drizzle-kit, no DB writes (read-only queries are fine), no package installs (report if one is truly needed), no git, no `next build` / `next dev`, no new tests.
4. **Verify** before finishing: `npx tsc --noEmit --incremental false 2>&1 | grep -E 'two-factor|settings/security|components/security|\(auth\)|workspaces/\[slug\]/(security|audit|page)|settings/audit|components/audit|lib/audit|api/audit|sso|scim|lib/auth\.ts'` shows zero errors in your files (others are mid-flight; ignore their errors), and `npx eslint <your files>` is clean.
5. Match the codebase: sparse comments that explain *why*; shadcn components in `src/components/ui`; domain glyphs in `src/components/ui-icons`; shared pickers in `src/components/issue-pickers`; lucide-react; Tailwind v4 tokens; sonner toasts; hotkey registry `src/lib/hotkeys.ts` (supports `shift`); palette registry `src/lib/palette-commands.ts`. Linear look and feel: dense, quiet, keyboard-first, good empty/loading states.
6. **Security**: every server action/route authorizes (`authorizeProjectAction`, project/workspace access helpers, API auth) before touching data and validates every referenced id. Public endpoints (webhooks, Slack, OAuth, SCIM, cron) verify their own signatures/tokens with timing-safe compares and never leak other projects' data.
7. Issue writes go through `src/lib/issue-service.ts` (actor-based) or the session actions in `src/app/actions/tickets.ts`, so activity, notifications, Slack and webhooks see them. Custom events include `data.summary`, `key`, `title`.
8. $0 only: free tiers, no paid services. Every external integration must degrade gracefully when its env vars are unset (explain in the UI what to configure).

## Final report (last message)
Features done (plan doc paths); files created/modified; integration requests (exact file + change); env vars used; anything unfinished or deviations.

## Your features
Two-factor authentication · SAML SSO / SCIM · Audit log export

## You own
- `src/lib/auth.ts` (plugin options only — keep all existing behaviour and plugins), `src/app/(auth)/**`, new `src/app/login/two-factor/**` or `src/app/(auth)/login/two-factor/**` (the twoFactorClient redirects to `/login/two-factor`)
- `src/app/dashboard/settings/security/**`, `src/app/dashboard/workspaces/[slug]/{security,audit}/**` (stubs), `src/app/dashboard/workspaces/[slug]/page.tsx` (only to add links to Security / Audit log for admins), `src/app/dashboard/projects/[id]/settings/audit/**` (stub)
- new: `src/components/security/**`, `src/components/audit/**`, `src/lib/audit.ts`, `src/app/api/audit/**`, `src/app/actions/security.ts`, `src/lib/sso.ts`

## Build
- **2FA (TOTP)** via the Better Auth `twoFactor` plugin: Settings → Security: enable (password confirm → QR code from the TOTP URI (render QR as SVG without a new package — implement a tiny QR encoder only if unavoidable, otherwise show the otpauth URI + secret for manual entry and a "copy" button), verify first code, show backup codes once with copy/download), disable, regenerate backup codes. `/login/two-factor` page: TOTP code or backup code, "trust this device". Also show active sessions with revoke (Better Auth session APIs).
- **SAML/OIDC SSO** via `@better-auth/sso`: workspace owners/admins configure an IdP (SAML metadata XML or entry point + cert; or OIDC issuer/client) for an email domain on `/dashboard/workspaces/[slug]/security` (store the link in `workspace_sso`), with the SP metadata / ACS / callback URLs to copy; "Enforce SSO" (members with that domain can't use password login — enforce in the login flow and via an auth hook). Login page: "Continue with SSO" (email → `signIn.sso` by domain). New SSO users matching the domain are added to the workspace as members.
- **SCIM** via `@better-auth/scim`: generate/rotate a SCIM token per workspace (shown once) with the base URL; provisioned users become workspace members, deprovisioned users are removed from the workspace and its projects (use plugin hooks/options or a mapping via `workspace_sso.scimProviderId`). Document how to test with Okta/Keycloak free tiers.
- **Audit log**: project settings → Audit log and workspace → Audit log pages: filterable table over `activity` (actor, type, date range, issue) with project-level + issue events and auth/security events you add (2FA enabled/disabled, SSO config changes, member/role changes already exist); export CSV/JSON via `GET /api/audit?scope=project|workspace&id=…&format=csv|json&from&to` (admins only, streamed, capped 50k rows).
