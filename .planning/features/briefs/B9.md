You are agent **B9 workspace** in a multi-agent build of Linear-parity features for a Next.js 16 + Drizzle + Neon ticket system at /home/oscar/Projects/ticket-system (branch feat/linear-parity). About 11 other Wave B agents work IN PARALLEL in the same working tree, each owning different files. Wave A already built the foundation (data layer, workflow states, pickers, slots, navigation, settings scaffolds, stub pages).

## Read first
- `.planning/features/00-MASTER-PLAN.md` (naming, contracts, ownership — authoritative) and the Wave A plan docs in `.planning/features/A1-*.md`, `A2-*.md`
- `.planning/features/CONTRACTS.md` (exact exported APIs from Wave A — use these, don't reinvent them)
- `AGENTS.md`: this is **Next.js 16** — APIs differ from your training data. Check `node_modules/next/dist/docs/` whenever unsure (params are Promises, `after` from `next/server`, route handlers, metadata, caching).
- `CLAUDE.md` (stack notes) and `src/db/schema.ts` (already migrated — every table you need exists).

## Rules
1. **Plan doc first**: before implementing each feature, write `.planning/features/B9-<slug>.md` (goal, UX, data/actions, files, edge cases — concise, ~20-40 lines). One doc per checklist feature.
2. **Strict file ownership** (shared working tree!): edit only the files listed under "You own" plus new files you create inside your own directories. Every slot/stub file assigned to you is yours to overwrite. Never edit another agent's files, even to fix a type error — note it under "Integration requests" in your final report instead. Frozen hosts (don't edit): `src/components/issue-detail/issue-detail.tsx`, `src/components/issue-detail/issue-detail-pane.tsx`, `src/app/dashboard/projects/[id]/layout.tsx`, `src/components/app-shell/shell-frame.tsx`, `src/lib/issue-model.ts`, `src/lib/tickets.ts`, `src/lib/events.ts`, `src/lib/action-auth.ts`, `src/lib/project-data.ts`, `src/app/actions/tickets.ts`, `src/db/schema.ts` — unless listed under "You own".
3. Don't run drizzle-kit, don't write to the database, don't install packages (ask via Integration requests if you truly need one — prefer the platform), no git, no `next build` / `next dev`, no new tests. If you need a schema change, STOP that part and report it.
4. **Verify** before finishing: `npx tsc --noEmit --incremental false 2>&1 | grep -E 'workspaces|people|settings/workflow|settings/labels|settings/members|actions/workspaces|actions/workflow|actions/invite|actions/join|actions/members|app/invite|member-list|invite-panel|join-project-button|sidebar-workspaces|workspace-access|project-access'` must show zero errors in your files (other agents are mid-flight; ignore errors in their files). `npx eslint <your files>` clean.
5. Style: match the codebase — sparse comments that explain *why*, shadcn components in `src/components/ui` (radix-nova; Popover, Command, DropdownMenu, ContextMenu, Dialog, AlertDialog, Sheet, Tooltip, HoverCard, Tabs, Switch, Select, Calendar, Checkbox, RadioGroup, ToggleGroup, Table, ScrollArea, Progress, Collapsible), domain glyphs in `src/components/ui-icons` (StatusIcon/StateIcon, PriorityIcon, LabelChip, Avatar, EmptyState, Skeleton), the reusable pickers in `src/components/issue-pickers/*`, lucide-react, Tailwind v4 tokens, sonner toasts, hotkey registry `src/lib/hotkeys.ts`, palette registry `src/lib/palette-commands.ts`. Linear look & feel: dense, quiet, keyboard-first, good empty states and loading states.
6. **Security**: every server action / route handler authorizes via `authorizeProjectAction` (or the project-access helpers) before reading or writing project data, and validates that every referenced id belongs to that project. Public endpoints (webhooks, API, intake) authenticate with their own secret/token and never leak other projects' data.
7. Mutations that change issues go through the existing actions in `src/app/actions/tickets.ts` (or your own actions that call `emitIssueEvent`) so activity history, notifications, Slack and webhooks all see them.
8. Keep the free-tier constraints: no paid services, no cron beyond lazy/throttled work, no websockets.

## Final report (your last message)
Features done (with plan doc paths); files created/modified; integration requests (exact file + exact change needed) for files you don't own; env vars added; anything unfinished or deviations.

## Your features
Workspaces with multiple teams · Roles beyond owner/member (admin, guest) · Email invitations · Member profile pages · Custom workflow statuses (settings UI) · Labels (settings management UI)

## You own
- `src/app/dashboard/workspaces/**` except `initiatives/**` (B8), `src/app/dashboard/people/**`
- `src/app/dashboard/projects/[id]/settings/workflow/**`, `.../settings/labels/**`, `.../settings/members/**`
- `src/app/actions/workspaces.ts`, `src/app/actions/workflow-states.ts` (new), `src/app/actions/invite.ts`, `src/app/actions/join.ts`, `src/app/actions/members.ts`
- `src/app/invite/**`, `src/components/member-list.tsx`, `src/components/invite-panel.tsx`, `src/components/join-project-button.tsx`, `src/components/workspaces/**`, `src/components/settings/workflow/**`, `src/components/settings/labels/**`, `src/components/people/**` (new)
- `src/components/app-shell/slots/sidebar-workspaces.tsx`
- `src/lib/workspace-access.ts` (pre-created — extend, keep exports stable; B8 uses it), additive helpers in `src/lib/project-access.ts` (don't change existing signatures)
- Use (don't edit) `src/app/actions/labels.ts` for label CRUD.

## Build
- **Workspaces**: "New workspace" (name → slug), workspace page `/dashboard/workspaces/[slug]` with its projects (teams), members (roles owner/admin/member), "Add project" (projects you own/admin; moving sets `project.workspaceId`), "Create project in workspace", invite workspace members (existing users by email among people you share projects with, or email invite), leave/delete workspace (owner). Sidebar `SidebarWorkspaces`: each workspace with links to its page and Initiatives (`/dashboard/workspaces/[slug]/initiatives`, B8's page).
- **Roles**: member list shows roles with a select — owner can set admin/member/guest, admins can set member/guest, nobody can demote the owner; "Transfer ownership" (owner only, confirm). Server enforcement already exists in `authorizeProjectAction` levels (guest = read + comment); make sure the member/invite actions enforce who may change what. Add a client helper hook `useProjectPermissions()` in `src/components/workspaces/permissions.ts` (derives from `useProjectData().project.role`) for UI gating and use it in your own screens.
- **Email invitations**: in the invite panel add "Invite by email" (email + role) → `invitation` row with `email`, `role`, `invitedById`, 7-day expiry, and `sendEmail` with the join link; pending invitations list (resend, revoke). The existing shareable link keeps working (role member). Accepting (`/invite/[token]` + `join.ts`): email invites require the signed-in user's email to match (case-insensitive), apply the invitation's role, set `acceptedAt`, single use; keep the existing logged-out join path working.
- **Member profile** `/dashboard/people/[userId]`: visible only if you share at least one project (or are the user); avatar, name, title, bio, timezone with local time, shared projects, open issues assigned to them in shared projects (link to permalinks), recent activity in shared projects. Link member avatars/names in your own member lists to it.
- **Workflow settings** `/projects/[id]/settings/workflow` (admin): states grouped by type (Triage, Backlog, Unstarted, Started, Completed, Canceled) with name, color (preset swatches + hex), description, reorder within type (up/down or drag → `position`), add state to a type, delete (must pick a replacement state for its issues; each type except triage keeps ≥ 1 state; the default states can be deleted like any other). Actions in `workflow-states.ts` emit project-level activity (`ticketId: null`).
- **Labels settings** `/projects/[id]/settings/labels`: list with color, name, description, issue count, search; create / edit (inline) / delete (confirm with count) via `src/app/actions/labels.ts`.

## Notes from Wave A (do these too)
- `src/app/actions/members.ts` and `src/app/actions/invite.ts` still `revalidatePath` the old `/dashboard/projects/${id}/members`; change to `/settings/members` (the old route redirects).
- `src/components/member-list.tsx` role type is still `'owner' | 'member'`; the settings members page (A2's, now yours) maps admin/guest to member — widen the type and remove that mapping.
- Settings actions from A2 use `roleAllows` from `src/lib/roles.ts`; reuse it.
