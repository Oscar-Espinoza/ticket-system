// Stub — owned by D10a
import type { Metadata } from 'next';

import { ComingSoon } from '@/components/coming-soon';

export const metadata: Metadata = { title: 'OAuth apps' };

export default function Page() {
  return <ComingSoon title="OAuth apps" />;
}
