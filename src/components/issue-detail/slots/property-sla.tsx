'use client';

// Slot — owned by D5. SLA countdown/breach row + "time in status" row.

import type { IssueMutations } from '@/components/issues/use-issue-mutations';
import type { IssueRow } from '@/lib/issue-model';

export function PropertySla(_props: { issue: IssueRow; mutations: IssueMutations }) {
  return null;
}
