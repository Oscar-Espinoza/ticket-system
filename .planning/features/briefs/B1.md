You are agent **B1 collaboration** in a multi-agent build of Linear-parity features for a Next.js 16 + Drizzle + Neon ticket system at /home/oscar/Projects/ticket-system (branch feat/linear-parity). About 11 other Wave B agents work IN PARALLEL in the same working tree, each owning different files. Wave A already built the foundation (data layer, workflow states, pickers, slots, navigation, settings scaffolds, stub pages).

## Read first
- `.planning/features/00-MASTER-PLAN.md` (naming, contracts, ownership — authoritative) and the Wave A plan docs in `.planning/features/A1-*.md`, `A2-*.md`
- `.planning/features/CONTRACTS.md` (exact exported APIs from Wave A — use these, don't reinvent them)
- `AGENTS.md`: this is **Next.js 16** — APIs differ from your training data. Check `node_modules/next/dist/docs/` whenever unsure (params are Promises, `after` from `next/server`, route handlers, metadata, caching).
- `CLAUDE.md` (stack notes) and `src/db/schema.ts` (already migrated — every table you need exists).

## Rules
1. **Plan doc first**: before implementing each feature, write `.planning/features/B1-<slug>.md` (goal, UX, data/actions, files, edge cases — concise, ~20-40 lines). One doc per checklist feature.
2. **Strict file ownership** (shared working tree!): edit only the files listed under "You own" plus new files you create inside your own directories. Every slot/stub file assigned to you is yours to overwrite. Never edit another agent's files, even to fix a type error — note it under "Integration requests" in your final report instead. Frozen hosts (don't edit): `src/components/issue-detail/issue-detail.tsx`, `src/components/issue-detail/issue-detail-pane.tsx`, `src/app/dashboard/projects/[id]/layout.tsx`, `src/components/app-shell/shell-frame.tsx`, `src/lib/issue-model.ts`, `src/lib/tickets.ts`, `src/lib/events.ts`, `src/lib/action-auth.ts`, `src/lib/project-data.ts`, `src/app/actions/tickets.ts`, `src/db/schema.ts` — unless listed under "You own".
3. Don't run drizzle-kit, don't write to the database, don't install packages (ask via Integration requests if you truly need one — prefer the platform), no git, no `next build` / `next dev`, no new tests. If you need a schema change, STOP that part and report it.
4. **Verify** before finishing: `npx tsc --noEmit --incremental false 2>&1 | grep -E 'editor/|comments|subscri|section-activity|description-editor|header-subscribe|lib/timeline'` must show zero errors in your files (other agents are mid-flight; ignore errors in their files). `npx eslint <your files>` clean.
5. Style: match the codebase — sparse comments that explain *why*, shadcn components in `src/components/ui` (radix-nova; Popover, Command, DropdownMenu, ContextMenu, Dialog, AlertDialog, Sheet, Tooltip, HoverCard, Tabs, Switch, Select, Calendar, Checkbox, RadioGroup, ToggleGroup, Table, ScrollArea, Progress, Collapsible), domain glyphs in `src/components/ui-icons` (StatusIcon/StateIcon, PriorityIcon, LabelChip, Avatar, EmptyState, Skeleton), the reusable pickers in `src/components/issue-pickers/*`, lucide-react, Tailwind v4 tokens, sonner toasts, hotkey registry `src/lib/hotkeys.ts`, palette registry `src/lib/palette-commands.ts`. Linear look & feel: dense, quiet, keyboard-first, good empty states and loading states.
6. **Security**: every server action / route handler authorizes via `authorizeProjectAction` (or the project-access helpers) before reading or writing project data, and validates that every referenced id belongs to that project. Public endpoints (webhooks, API, intake) authenticate with their own secret/token and never leak other projects' data.
7. Mutations that change issues go through the existing actions in `src/app/actions/tickets.ts` (or your own actions that call `emitIssueEvent`) so activity history, notifications, Slack and webhooks all see them.
8. Keep the free-tier constraints: no paid services, no cron beyond lazy/throttled work, no websockets.

## Final report (your last message)
Features done (with plan doc paths); files created/modified; integration requests (exact file + exact change needed) for files you don't own; env vars added; anything unfinished or deviations.

## Your features (checklist items)
Rich-text / markdown editor · Comments · Comment reactions · @mentions · Activity history / audit log · Subscribers / follow issue

## You own
- `src/components/editor/**` (new) — `MarkdownEditor` + `Markdown` renderer
- `src/components/comments/**` (new)
- `src/components/issue-detail/slots/description-editor.tsx`, `section-activity.tsx`, `header-subscribe.tsx`
- `src/app/actions/comments.ts`, `src/app/actions/subscriptions.ts` (new)
- `src/lib/comments.ts`, `src/lib/timeline.ts` (new), and `src/lib/subscriptions.ts`, `src/lib/mentions.ts` (pre-created shared helpers — you may extend, keep existing exports stable: B2 imports them)

## Build
- **Editor**: markdown with a quiet toolbar (bold, italic, code, link, bullet/numbered/check list, quote, code block), shortcuts ⌘B/⌘I/⌘K/⌘E, ⌘Enter submit, Esc cancel, write/preview toggle, autosize. **@mention** autocomplete popover over project members (from `useProjectData().members`), inserting the canonical token from `src/lib/mentions.ts`. `Markdown` renderer: `react-markdown` + `remark-gfm` (installed), no raw HTML, safe links (`rel="noreferrer noopener"`, external target), task-list checkboxes, mentions rendered as chips, issue keys like `APP-12` auto-linked to `/dashboard/projects/<id>/issues/APP-12` when the key matches the current project (pass projectId/ticketKey via props or context). Export both so other agents (epics, inbox, comments) can reuse them.
- **Description**: replace the slot's textarea with a click-to-edit markdown view/edit (save on blur/⌘Enter via `mutations.update`), keep the empty placeholder. Description @mentions should notify: emit a `description.mentioned` event with `data.mentions` for newly added mentions (diff old vs new) through `emitIssueEvent` in a small server action.
- **Comments** in `SectionActivity`: composer at the bottom, threaded replies (one level, `comment.parentId`), edit (shows "edited") / delete own comments (admins can delete any), reactions (emoji set 👍 👎 ❤️ 🎉 😄 😕 👀 🚀 as toggles with counts + who reacted tooltip), permalink anchor per comment. Comment actions use `authorizeProjectAction(projectId, 'comment')`, emit `comment.created` (data: `{ commentId, excerpt, mentions }`), `comment.updated`, `comment.deleted`; commenters and mentioned users are auto-subscribed (`ensureSubscribed`).
- **Activity history**: one chronological timeline merging `activity` rows (human sentences for `issue.created`, `issue.updated` changes — state/priority/assignee/labels/estimate/due date/parent/cycle/epic/title/description —, archive/restore/delete, and any other type via its `data.summary` string, falling back to "updated the issue") with comments; actor avatar + relative time with absolute tooltip; collapse runs of >5 minor events ("show N more"). Load via a server action `getIssueTimeline(projectId, ticketId)` (authorize read) called from the client when the issue opens and after local mutations (refetch on `issue.updatedAt` change).
- **Subscribers**: header bell toggle (Subscribe / Unsubscribe) + popover listing subscribers with avatars; actions in `subscriptions.ts`.

## Notes from Wave A
- `DescriptionEditor` currently accepts `readOnly` (guests) — keep that prop working.
- Activity data shapes: see `IssueChange` in `src/lib/events.ts`; description changes are logged with from/to null.
